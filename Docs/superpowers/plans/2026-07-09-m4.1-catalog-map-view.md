# M4.1 — Catalog Map View (list⇄map toggle) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a list⇄map toggle to the two catalog pages, where the map shows a pin per city (with a listing-count badge) for the current category/filter, and clicking a pin filters/navigates to that city.

**Architecture:** City-level positioning only (28 hardcoded Bulgarian oblast-capital coordinates — no DB migration, no per-listing geodata). A new `PublicListingDAL.countByCity(input)` aggregate groups the current filter's published listings by city; whole-country listings are excluded from pins (they have no map position — see `CONTEXT.md` «Гео-локация (град)»). Leaflet + OpenStreetMap tiles render client-side via a `next/dynamic(..., { ssr: false })` wrapper (Leaflet touches `window` at module load, so it must not run during SSR). The catalog page files stay Server Components and branch their JSX on `?view=map`.

**Tech Stack:** Next.js 16.2.10 (App Router, forked — read `node_modules/next/dist/docs/` before Next-API code), React 19.2.4, `leaflet` + `react-leaflet` v5 (client-only), Drizzle + neon-serverless, next-intl, Tailwind v4, shadcn/ui, vitest.

## Global Constraints

- **Forked Next.js:** before writing any Next-API code, read the relevant guide under `node_modules/next/dist/docs/` (per `AGENTS.md`). Reading those files may be permission-blocked for `Bash cat`/`Read` in this sandbox — use `python3 -c "print(open('...').read())"` if so. `next/dynamic(..., { ssr: false })` **must** be called from inside a `"use client"` component, never directly in a Server Component page.
- **Secrets:** never touch `.env`/`.env.example` in this milestone (no secrets involved).
- **Git:** `git add` specific paths only, never `-A`.
- **DAL convention:** public read methods return DTOs; no side effects. `PublicListingDAL` filter conditions are built inline per method — this plan extracts the shared block into one `baseConds()` helper reused by `list()` and the new `countByCity()`, so the two WHERE clauses cannot drift.
- **City granularity:** listings position at their city's fixed coordinates; a `wholeCountry` listing has **no pin** and must be excluded from `countByCity`. City filter param is `?city=<uuid>` (a city **id**, validated by `uuidParam`) — not a slug. On `/[category]/[city]` the city comes from the route segment (slug), resolved server-side.
- **i18n:** every user-facing string added under the existing `"Catalog"` namespace in **both** `messages/bg.json` and `messages/en.json`. `bg` is the source-of-truth wording.
- **OSM attribution:** the tile layer must render the required `© OpenStreetMap contributors` attribution.
- **Cache:** new cached read wrappers use `unstable_cache` with tag `"listings"` and 1h revalidate, mirroring `public-cached.ts`.

## File Structure

- Create `src/data/catalog/city-coords.ts` — static `CITY_COORDS: Record<slug, [lat, lng]>` (28 entries) + coverage helper.
- Modify `src/data/catalog/public.dto.ts` — add `PublicListingCityCount` type.
- Modify `src/data/catalog/public-listing.dal.ts` — extract `baseConds()`, refactor `list()`, add `countByCity()`.
- Modify `src/data/catalog/public-cached.ts` — add `cachedListingCountByCity()`.
- Create `src/data/catalog/public-listing-count-by-city.dal.test.ts` — DB test.
- Create `src/data/catalog/city-coords.test.ts` — coverage test.
- Create `src/lib/map-links.ts` — pure `cityPinHref()` URL builder (no Leaflet import).
- Create `src/lib/map-links.test.ts` — unit test.
- Create `src/components/catalog/listing-map.tsx` — `"use client"` dynamic wrapper.
- Create `src/components/catalog/listing-map-inner.tsx` — react-leaflet map + pins.
- Create `src/components/catalog/view-toggle.tsx` — list/map toggle.
- Modify `messages/bg.json`, `messages/en.json` — `Catalog.viewList`/`viewMap`/`viewToggleLabel`.
- Modify `src/lib/catalog-search-params.ts` — add `parseView()`.
- Modify `src/app/[locale]/(public)/[category]/page.tsx` and `.../[category]/[city]/page.tsx` — wire toggle + map branch.
- Modify `package.json` — add `leaflet`, `react-leaflet`, `@types/leaflet`.

---

### Task 1: `countByCity` aggregate — DTO, shared `baseConds()`, DAL method, cached wrapper

**Files:**
- Modify: `src/data/catalog/public.dto.ts` (add type after `PublicListingPage`, ~line 64)
- Modify: `src/data/catalog/public-listing.dal.ts` (extract helper; refactor `list()` at 171–226; add `countByCity()`)
- Modify: `src/data/catalog/public-cached.ts` (add wrapper after `cachedListingList`, ~line 24)
- Test: `src/data/catalog/public-listing-count-by-city.dal.test.ts`

**Interfaces:**
- Produces: `PublicListingCityCount = { cityId: string; slug: string; name: string; count: number }`
- Produces: `PublicListingDAL.countByCity(input: PublicListingFilterInput): Promise<PublicListingCityCount[]>` — groups **published, non-`wholeCountry`** listings matching every filter in `input` **except `cityId`** (the city filter is intentionally ignored so pins span all cities of the category), by `city`. Returns one row per city that has ≥1 matching listing.
- Produces: `cachedListingCountByCity(input): Promise<PublicListingCityCount[]>`
- Consumes: existing `PublicListingFilterInput`, `db`, `listing`, `city` schema, `and/eq/count/sql` from drizzle.

- [ ] **Step 1: Write the failing test**

Create `src/data/catalog/public-listing-count-by-city.dal.test.ts`. Mirror the harness of `public-listing-list.dal.test.ts` exactly (shared dev Neon → assert only against this test's uniquely-priced seed, never global counts). Use a **unique sentinel price** so rows are deterministic despite parallel data:

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import { eq } from "drizzle-orm";
import { createTestUser, cleanupTestUser, createTestSubscription, testDb } from "@/test/db-helpers";
import * as schema from "@/db/schema";
import { ListingDAL } from "./listing.dal";
import { PackageDAL } from "./package.dal";
import type { SessionUser } from "@/data/users/require-user";
import type { PublicListingFilterInput } from "./public.dto";

// Уникална sentinel цена — никоя друга обява в споделената dev Neon не струва точно това,
// затова countByCity(price=PIN) връща само seed-натите тук редове, детерминистично.
const PIN = 424243;
let owner1Id: string, owner2Id: string, categoryId: string;
let cityA: string, cityB: string, cityC: string;
let cityASlug: string, cityBSlug: string, cityCSlug: string;

const base = (over: Partial<PublicListingFilterInput>): PublicListingFilterInput => ({
  categoryId, sort: "new", page: 1, perPage: 24, priceMinCents: PIN, priceMaxCents: PIN, ...over,
});

beforeAll(async () => {
  // premium планът позволява само 2 published/категория/owner (billing.dal.ts assertCanPublish),
  // затова 4-те fotografi обяви се разпределят 2+2 между двама owner-а (както sibling list-теста).
  const u1 = await createTestUser();
  owner1Id = u1.id;
  const owner1: SessionUser = { id: u1.id, email: u1.email, name: "Тест", isAdmin: false };
  const u2 = await createTestUser();
  owner2Id = u2.id;
  const owner2: SessionUser = { id: u2.id, email: u2.email, name: "Тест 2", isAdmin: false };
  await createTestSubscription(owner1Id, { plan: "premium", status: "active" });
  await createTestSubscription(owner2Id, { plan: "premium", status: "active" });
  const [cat] = await testDb.select().from(schema.category).where(eq(schema.category.slug, "fotografi"));
  categoryId = cat!.id;
  const cities = await testDb.select().from(schema.city).limit(3);
  cityA = cities[0]!.id; cityASlug = cities[0]!.slug;
  cityB = cities[1]!.id; cityBSlug = cities[1]!.slug;
  cityC = cities[2]!.id; cityCSlug = cities[2]!.slug;

  const mk = async (owner: SessionUser, cityId: string, wholeCountry = false) => {
    const l = await ListingDAL.for(owner).createDraft({ title: "Обява", categoryId, cityId });
    await PackageDAL.for(owner).create({ listingId: l.id, name: "П", priceFromCents: PIN });
    await ListingDAL.for(owner).submit(l.id);
    await testDb.update(schema.listing)
      .set({ status: "published", publishedAt: new Date(), wholeCountry })
      .where(eq(schema.listing.id, l.id));
    return l;
  };
  await mk(owner1, cityA);            // 2 в cityA (owner1, на cap)
  await mk(owner1, cityA);
  await mk(owner2, cityB);            // 1 в cityB (owner2)
  await mk(owner2, cityC, true);      // cityC е wholeCountry → БЕЗ пин (owner2, на cap)
});

afterAll(async () => { await cleanupTestUser(owner1Id); await cleanupTestUser(owner2Id); });

test("countByCity: групира по град, брои коректно", async () => {
  const rows = await ListingDAL.public().countByCity(base({}));
  const byId = new Map(rows.map((r) => [r.cityId, r]));
  expect(byId.get(cityA)?.count).toBe(2);
  expect(byId.get(cityB)?.count).toBe(1);
  expect(byId.get(cityA)?.slug).toBe(cityASlug);
  expect(byId.get(cityB)?.name).toBeTruthy();
});

test("countByCity: wholeCountry обявите нямат пин", async () => {
  const rows = await ListingDAL.public().countByCity(base({}));
  expect(rows.some((r) => r.cityId === cityC)).toBe(false);
});

test("countByCity: игнорира cityId от input (пиновете покриват всички градове)", async () => {
  const rows = await ListingDAL.public().countByCity(base({ cityId: cityA }));
  // въпреки cityId=cityA, cityB пак присъства
  expect(rows.some((r) => r.cityId === cityB)).toBe(true);
});

test("countByCity: друга категория → без наши редове", async () => {
  const [dj] = await testDb.select().from(schema.category).where(eq(schema.category.slug, "dj"));
  const rows = await ListingDAL.public().countByCity(base({ categoryId: dj!.id }));
  expect(rows.some((r) => r.cityId === cityA || r.cityId === cityB)).toBe(false);
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/data/catalog/public-listing-count-by-city.dal.test.ts`
Expected: FAIL — `countByCity is not a function`.

- [ ] **Step 3: Add the DTO type**

In `src/data/catalog/public.dto.ts`, after `PublicListingPage` (line 64):

```ts
export type PublicListingCityCount = {
  cityId: string;
  slug: string;
  name: string;
  count: number;
};
```

- [ ] **Step 4: Extract `baseConds()` and refactor `list()`**

In `src/data/catalog/public-listing.dal.ts`, add a module-level helper **above** the class (after `cardColumns`/`toCard`, before the DAL class). It contains every condition from `list()` **except** `status`-scoping duplication is kept, but **excludes** the `cityId` condition (callers add city themselves):

```ts
// Споделен conds-builder за публичните листинг заявки. Съзнателно НЕ включва cityId —
// list() го добавя при филтър по град, countByCity() го пропуска (пиновете покриват всички градове).
function baseConds(input: PublicListingFilterInput): SQL[] {
  const conds: SQL[] = [
    eq(listing.status, "published"),
    eq(listing.categoryId, input.categoryId),
  ];
  if (input.regionId) {
    conds.push(sql`(${listing.wholeCountry} = true or exists (
      select 1 from ${listingServiceRegion} lsr
      where lsr.listing_id = ${listing.id} and lsr.region_id = ${input.regionId}
    ))`);
  }
  if (input.priceMinCents !== undefined) conds.push(gte(listing.priceFromCents, input.priceMinCents));
  if (input.priceMaxCents !== undefined) conds.push(lte(listing.priceFromCents, input.priceMaxCents));
  for (const attr of input.attrs ?? []) {
    if (attr.values.length === 0) continue;
    const vals = sql.join(attr.values.map((v) => sql`${v}`), sql`, `);
    conds.push(sql`exists (
      select 1 from ${listingAttribute} la
      where la.listing_id = ${listing.id}
        and la.attribute_definition_id = ${attr.definitionId}
        and la.value ?| array[${vals}]
    )`);
  }
  return conds;
}
```

Then replace the inline block in `list()` (current lines 176–201) with:

```ts
    const conds = baseConds(input);
    if (input.cityId) conds.push(eq(listing.cityId, input.cityId));
    const where = and(...conds);
```

Leave the `orderBy`, the two `db.select(...)` queries, and the return statement of `list()` untouched.

- [ ] **Step 5: Add `countByCity()`**

Add this method to the `PublicListingDAL` class (next to `list()`):

```ts
  async countByCity(input: PublicListingFilterInput): Promise<PublicListingCityCount[]> {
    const conds = baseConds(input);
    // wholeCountry обявите нямат позиция на картата (виж CONTEXT.md «Гео-локация (град)»)
    conds.push(eq(listing.wholeCountry, false));
    const rows = await db
      .select({ cityId: listing.cityId, slug: city.slug, name: city.name, cnt: count() })
      .from(listing)
      .innerJoin(city, eq(listing.cityId, city.id))
      .where(and(...conds))
      .groupBy(listing.cityId, city.slug, city.name);
    return rows.map((r) => ({ cityId: r.cityId, slug: r.slug, name: r.name, count: r.cnt }));
  }
```

Add `PublicListingCityCount` to the type import from `./public.dto` at the top of the file.

- [ ] **Step 6: Add the cached wrapper**

In `src/data/catalog/public-cached.ts`, after `cachedListingList` (line 24), and add `PublicListingCityCount` is not needed here (return type inferred):

```ts
export function cachedListingCountByCity(input: PublicListingFilterInput) {
  return unstable_cache(
    () => ListingDAL.public().countByCity(input),
    ["listing-count-by-city", JSON.stringify(input)],
    { tags: ["listings"], revalidate: HOUR },
  )();
}
```

- [ ] **Step 7: Run tests to verify they pass**

Run: `pnpm vitest run src/data/catalog/public-listing-count-by-city.dal.test.ts src/data/catalog/public-listing-list.dal.test.ts`
Expected: PASS (new file green; `list()` regression file still green).

- [ ] **Step 8: Commit**

```bash
git add src/data/catalog/public.dto.ts src/data/catalog/public-listing.dal.ts src/data/catalog/public-cached.ts src/data/catalog/public-listing-count-by-city.dal.test.ts
git commit -m "feat(catalog): countByCity aggregate for map pins"
```

---

### Task 2: Static city coordinates table

**Files:**
- Create: `src/data/catalog/city-coords.ts`
- Test: `src/data/catalog/city-coords.test.ts`

**Interfaces:**
- Produces: `CITY_COORDS: Record<string, readonly [number, number]>` — keyed by city **slug**, value `[lat, lng]`.

- [ ] **Step 1: Write the failing coverage test**

The test guards against drift: every seeded city slug (from `scripts/seed-data.ts` `REGIONS[].city.slug`) must have coordinates.

Create `src/data/catalog/city-coords.test.ts`:

```ts
import { expect, test } from "vitest";
import { REGIONS } from "../../../scripts/seed-data";
import { CITY_COORDS } from "./city-coords";

test("всеки seed-нат град има координати", () => {
  for (const r of REGIONS) {
    const [lat, lng] = CITY_COORDS[r.city.slug] ?? [];
    expect(CITY_COORDS[r.city.slug], `липсва координата за ${r.city.slug}`).toBeDefined();
    // България: lat ~41–44.3, lng ~22.3–28.7
    expect(lat).toBeGreaterThan(41);
    expect(lat).toBeLessThan(44.5);
    expect(lng).toBeGreaterThan(22);
    expect(lng).toBeLessThan(29);
  }
});
```

Note: verify the import path/name of `REGIONS` in `scripts/seed-data.ts` and adjust the relative import if the export differs.

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/data/catalog/city-coords.test.ts`
Expected: FAIL — cannot find `./city-coords`.

- [ ] **Step 3: Create the coordinates table**

Create `src/data/catalog/city-coords.ts`. 28 oblast capitals; approximate city-center coordinates are correct for a city-level pin.

```ts
// ponytail: 28 фиксирани точки — статична таблица е по-проста от DB миграция за непроменлив набор.
// Ключ = city.slug (виж scripts/seed-data.ts REGIONS). Стойност = [lat, lng] (център на града).
export const CITY_COORDS: Record<string, readonly [number, number]> = {
  blagoevgrad: [42.0208, 23.0942],
  burgas: [42.5048, 27.4626],
  varna: [43.2141, 27.9147],
  "veliko-tarnovo": [43.0757, 25.6172],
  vidin: [43.9855, 22.8722],
  vratsa: [43.2102, 23.5527],
  gabrovo: [42.8742, 25.3342],
  dobrich: [43.5726, 27.8273],
  kardzhali: [41.6339, 25.3766],
  kyustendil: [42.2833, 22.6914],
  lovech: [43.1370, 24.7140],
  montana: [43.4125, 23.2257],
  pazardzhik: [42.1928, 24.3336],
  pernik: [42.6050, 23.0378],
  pleven: [43.4170, 24.6067],
  plovdiv: [42.1354, 24.7453],
  razgrad: [43.5335, 26.5240],
  ruse: [43.8564, 25.9705],
  silistra: [44.1172, 27.2606],
  sliven: [42.6858, 26.3292],
  smolyan: [41.5775, 24.7124],
  sofia: [42.6977, 23.3219],
  samokov: [42.3378, 23.5550],
  "stara-zagora": [42.4258, 25.6345],
  targovishte: [43.2513, 26.5720],
  haskovo: [41.9344, 25.5554],
  shumen: [43.2712, 26.9361],
  yambol: [42.4842, 26.5035],
};
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run src/data/catalog/city-coords.test.ts`
Expected: PASS (28 slugs covered). If it fails on an unknown slug, the seed has a city not in the table — add its coordinates.

- [ ] **Step 5: Commit**

```bash
git add src/data/catalog/city-coords.ts src/data/catalog/city-coords.test.ts
git commit -m "feat(catalog): static city coordinates table for map pins"
```

---

### Task 3: `cityPinHref` pure URL builder

**Files:**
- Create: `src/lib/map-links.ts`
- Test: `src/lib/map-links.test.ts`

**Interfaces:**
- Produces:
  ```ts
  type PinLinkMode = "query" | "route";
  function cityPinHref(args: {
    mode: PinLinkMode;
    pin: { cityId: string; slug: string };
    categorySlug: string;
    currentParams: URLSearchParams; // used only in "query" mode
  }): string;
  ```
  - `"query"` mode (`/[category]` page): returns `` `/${categorySlug}?${qs}` `` where `qs` is a copy of `currentParams` with `city` set to `pin.cityId`, and `page`+`view` **deleted** (return to the list of that city).
  - `"route"` mode (`/[category]/[city]` page): returns `` `/${categorySlug}/${pin.slug}` `` (locale prefix is added by the locale-aware router at call site — this returns the unprefixed path).

This is the one pure, unit-tested piece of the click behavior; the Leaflet component (Task 4) imports and calls it so the map file needs no URL logic of its own. No Leaflet import here → clean to test.

- [ ] **Step 1: Write the failing test**

Create `src/lib/map-links.test.ts`:

```ts
import { expect, test } from "vitest";
import { cityPinHref } from "./map-links";

test("query mode: слага city, маха page и view", () => {
  const params = new URLSearchParams("sort=priceAsc&page=3&view=map&priceMin=1000");
  const href = cityPinHref({
    mode: "query",
    pin: { cityId: "11111111-1111-1111-1111-111111111111", slug: "plovdiv" },
    categorySlug: "fotografi",
    currentParams: params,
  });
  const [path, qs] = href.split("?");
  expect(path).toBe("/fotografi");
  const out = new URLSearchParams(qs);
  expect(out.get("city")).toBe("11111111-1111-1111-1111-111111111111");
  expect(out.get("sort")).toBe("priceAsc");
  expect(out.get("priceMin")).toBe("1000");
  expect(out.has("page")).toBe(false);
  expect(out.has("view")).toBe(false);
});

test("query mode: без други params → само city", () => {
  const href = cityPinHref({
    mode: "query",
    pin: { cityId: "abc", slug: "varna" },
    categorySlug: "dj",
    currentParams: new URLSearchParams(),
  });
  expect(href).toBe("/dj?city=abc");
});

test("route mode: към geo-landing по slug, игнорира params", () => {
  const href = cityPinHref({
    mode: "route",
    pin: { cityId: "abc", slug: "varna" },
    categorySlug: "dj",
    currentParams: new URLSearchParams("view=map"),
  });
  expect(href).toBe("/dj/varna");
});
```

- [ ] **Step 2: Run test to verify it fails**

Run: `pnpm vitest run src/lib/map-links.test.ts`
Expected: FAIL — cannot find `./map-links`.

- [ ] **Step 3: Implement**

Create `src/lib/map-links.ts`:

```ts
export type PinLinkMode = "query" | "route";

export function cityPinHref(args: {
  mode: PinLinkMode;
  pin: { cityId: string; slug: string };
  categorySlug: string;
  currentParams: URLSearchParams;
}): string {
  const { mode, pin, categorySlug, currentParams } = args;
  if (mode === "route") return `/${categorySlug}/${pin.slug}`;
  const params = new URLSearchParams(currentParams.toString());
  params.set("city", pin.cityId);
  params.delete("page");
  params.delete("view");
  const qs = params.toString();
  return qs ? `/${categorySlug}?${qs}` : `/${categorySlug}`;
}
```

- [ ] **Step 4: Run test to verify it passes**

Run: `pnpm vitest run src/lib/map-links.test.ts`
Expected: PASS (3 tests).

- [ ] **Step 5: Commit**

```bash
git add src/lib/map-links.ts src/lib/map-links.test.ts
git commit -m "feat(catalog): cityPinHref pure URL builder for map pins"
```

---

### Task 4: Leaflet install + map components (crux — SSR-safe dynamic import)

**Files:**
- Modify: `package.json` (deps)
- Create: `src/components/catalog/listing-map-inner.tsx`
- Create: `src/components/catalog/listing-map.tsx`

**Interfaces:**
- Consumes: `PublicListingCityCount[]` (Task 1), `CITY_COORDS` (Task 2), `cityPinHref` + `PinLinkMode` (Task 3).
- Produces:
  ```ts
  function ListingMap(props: {
    pins: PublicListingCityCount[];
    mode: PinLinkMode;
    categorySlug: string;
    activeCitySlug?: string;
  }): JSX.Element;
  ```
  Default-exported dynamic behavior is internal; pages import the named `ListingMap`.

**Verification note:** Leaflet requires a real browser DOM; there is no meaningful vitest for the rendered map. This task is verified by (a) `pnpm exec tsc --noEmit` passing and (b) `pnpm build` succeeding — the build is the real gate that the `ssr: false` wrapper prevents Leaflet from executing during SSR (a broken wrapper throws `window is not defined` at build/prerender). The click-URL logic is already unit-tested in Task 3.

- [ ] **Step 1: Read the forked-Next lazy-loading guide**

Read `node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md` (use `python3 -c "print(open('node_modules/next/dist/docs/01-app/02-guides/lazy-loading.md').read())"` if `Read`/`cat` is permission-blocked). Confirm the `ssr: false` "Skipping SSR" pattern and that it must live in a Client Component.

- [ ] **Step 2: Install dependencies**

```bash
pnpm add leaflet react-leaflet && pnpm add -D @types/leaflet
```
Expected: `react-leaflet` v5.x (React 19 compatible), `leaflet` v1.9.x. Verify `package.json` now lists all three.

- [ ] **Step 3: Write the inner map component**

Create `src/components/catalog/listing-map-inner.tsx`. Uses `divIcon` count badges (avoids Leaflet's broken default-marker-asset problem entirely — no image imports needed). Tailwind classes in the `html` string are static literals so the JIT picks them up; keep both active/normal class strings literal.

```tsx
"use client";
import "leaflet/dist/leaflet.css";
import L from "leaflet";
import { MapContainer, TileLayer, Marker } from "react-leaflet";
import { useRouter } from "@/i18n/navigation";
import { useSearchParams } from "next/navigation";
import { CITY_COORDS } from "@/data/catalog/city-coords";
import { cityPinHref, type PinLinkMode } from "@/lib/map-links";
import type { PublicListingCityCount } from "@/data/catalog/public.dto";

// център на България + zoom, покриващ цялата страна
const BG_CENTER: [number, number] = [42.73, 25.48];
const BG_ZOOM = 7;

function badge(count: number, active: boolean): L.DivIcon {
  const cls = active
    ? "flex h-8 min-w-8 items-center justify-center rounded-full border-2 border-white bg-foreground px-2 text-xs font-semibold text-background shadow-md"
    : "flex h-8 min-w-8 items-center justify-center rounded-full border-2 border-white bg-primary px-2 text-xs font-semibold text-primary-foreground shadow-md";
  return L.divIcon({
    className: "",
    html: `<div class="${cls}">${count}</div>`,
    iconSize: [32, 32],
    iconAnchor: [16, 16],
  });
}

export function ListingMapInner({
  pins,
  mode,
  categorySlug,
  activeCitySlug,
}: {
  pins: PublicListingCityCount[];
  mode: PinLinkMode;
  categorySlug: string;
  activeCitySlug?: string;
}) {
  const router = useRouter();
  const searchParams = useSearchParams();
  return (
    <MapContainer
      center={BG_CENTER}
      zoom={BG_ZOOM}
      scrollWheelZoom
      className="h-[70vh] min-h-[420px] w-full rounded-lg"
    >
      <TileLayer
        url="https://tile.openstreetmap.org/{z}/{x}/{y}.png"
        attribution='&copy; <a href="https://www.openstreetmap.org/copyright">OpenStreetMap</a> contributors'
      />
      {pins.map((pin) => {
        const coords = CITY_COORDS[pin.slug];
        if (!coords) return null; // няма координата → без пин (не би трябвало за seed-натите 28)
        return (
          <Marker
            key={pin.cityId}
            position={coords as [number, number]}
            icon={badge(pin.count, pin.slug === activeCitySlug)}
            eventHandlers={{
              click: () =>
                router.push(cityPinHref({ mode, pin, categorySlug, currentParams: searchParams })),
            }}
          />
        );
      })}
    </MapContainer>
  );
}
```

Notes for the implementer:
- `useRouter` from `@/i18n/navigation` (locale-aware) accepts the unprefixed path from `cityPinHref` and adds the locale prefix.
- `useSearchParams` from `next/navigation` returns a `ReadonlyURLSearchParams`, which `cityPinHref` copies via `.toString()` — compatible.
- If TypeScript complains that `react-leaflet`'s `MapContainer` `center`/`zoom` props are missing under React 19 types, confirm react-leaflet v5 is installed (v4 has stricter/older typings). Do not downgrade React.

- [ ] **Step 4: Write the client dynamic wrapper**

Create `src/components/catalog/listing-map.tsx`:

```tsx
"use client";
import dynamic from "next/dynamic";
import type { PinLinkMode } from "@/lib/map-links";
import type { PublicListingCityCount } from "@/data/catalog/public.dto";

const Inner = dynamic(
  () => import("./listing-map-inner").then((m) => m.ListingMapInner),
  {
    ssr: false,
    loading: () => <div className="h-[70vh] min-h-[420px] w-full animate-pulse rounded-lg bg-muted" />,
  },
);

export function ListingMap(props: {
  pins: PublicListingCityCount[];
  mode: PinLinkMode;
  categorySlug: string;
  activeCitySlug?: string;
}) {
  return <Inner {...props} />;
}
```

- [ ] **Step 5: Verify typecheck + build**

Run: `pnpm exec tsc --noEmit`
Expected: no errors.

Run: `pnpm build`
Expected: build succeeds; **no** `window is not defined` / `document is not defined` error during prerender (proves `ssr: false` isolates Leaflet). If it fails with `window is not defined`, the dynamic wrapper is wrong — Leaflet is being SSR'd; fix before proceeding.

- [ ] **Step 6: Commit**

```bash
git add package.json pnpm-lock.yaml src/components/catalog/listing-map-inner.tsx src/components/catalog/listing-map.tsx
git commit -m "feat(catalog): Leaflet city-pin map (SSR-safe dynamic import)"
```

---

### Task 5: View toggle component + i18n

**Files:**
- Create: `src/components/catalog/view-toggle.tsx`
- Modify: `messages/bg.json`, `messages/en.json` (`Catalog` namespace)

**Interfaces:**
- Produces: `function ViewToggle(props: { view: "list" | "map" }): JSX.Element` — a two-button group; selecting "map" sets `?view=map` + deletes `page`; selecting "list" deletes `view` + `page`. Mirrors `catalog-sort.tsx`'s router pattern (`useRouter`/`usePathname` from `@/i18n/navigation`, `useSearchParams` from `next/navigation`, `router.replace`).

- [ ] **Step 1: Add i18n keys**

In `messages/bg.json`, inside the `"Catalog"` object (starts line 136), add:

```json
"viewList": "Списък",
"viewMap": "Карта",
"viewToggleLabel": "Изглед на резултатите"
```

In `messages/en.json`, inside the matching `"Catalog"` object, add:

```json
"viewList": "List",
"viewMap": "Map",
"viewToggleLabel": "Results view"
```

Keep the byte-mirrored key ordering consistent with the file's existing convention (add the three keys at the same position in both files).

- [ ] **Step 2: Create the toggle**

Create `src/components/catalog/view-toggle.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import { useSearchParams } from "next/navigation";
import { usePathname, useRouter } from "@/i18n/navigation";
import { Button } from "@/components/ui/button";

export function ViewToggle({ view }: { view: "list" | "map" }) {
  const t = useTranslations("Catalog");
  const router = useRouter();
  const pathname = usePathname();
  const searchParams = useSearchParams();

  function go(next: "list" | "map") {
    if (next === view) return;
    const params = new URLSearchParams(searchParams.toString());
    if (next === "map") params.set("view", "map");
    else params.delete("view");
    params.delete("page");
    const qs = params.toString();
    router.replace(qs ? `${pathname}?${qs}` : pathname);
  }

  return (
    <div className="inline-flex h-11 rounded-md border border-border p-0.5" role="group" aria-label={t("viewToggleLabel")}>
      <Button
        type="button"
        variant={view === "list" ? "default" : "ghost"}
        size="sm"
        className="h-full rounded-sm"
        aria-pressed={view === "list"}
        onClick={() => go("list")}
      >
        {t("viewList")}
      </Button>
      <Button
        type="button"
        variant={view === "map" ? "default" : "ghost"}
        size="sm"
        className="h-full rounded-sm"
        aria-pressed={view === "map"}
        onClick={() => go("map")}
      >
        {t("viewMap")}
      </Button>
    </div>
  );
}
```

Note: confirm `Button` is exported from `@/components/ui/button` with `variant`/`size` props (shadcn default). If the variant names differ in this repo, match the existing ones used elsewhere (e.g. in `filters-panel.tsx`).

- [ ] **Step 3: Verify typecheck**

Run: `pnpm exec tsc --noEmit`
Expected: no errors.

- [ ] **Step 4: Commit**

```bash
git add src/components/catalog/view-toggle.tsx messages/bg.json messages/en.json
git commit -m "feat(catalog): list/map view toggle + i18n"
```

---

### Task 6: Wire map into both catalog pages

**Files:**
- Modify: `src/lib/catalog-search-params.ts` (add `parseView`)
- Modify: `src/app/[locale]/(public)/[category]/page.tsx`
- Modify: `src/app/[locale]/(public)/[category]/[city]/page.tsx`

**Interfaces:**
- Consumes: `cachedListingCountByCity` (Task 1), `ListingMap` (Task 4), `ViewToggle` (Task 5).
- Produces: `parseView(sp): "list" | "map"`.

**Verification note:** Server-component page wiring is verified by `pnpm exec tsc --noEmit` + `pnpm build` + a dev-server smoke check (both pages render list by default and map on `?view=map`). No vitest.

- [ ] **Step 1: Add `parseView`**

In `src/lib/catalog-search-params.ts`, after `parseSort` (line 31):

```ts
export function parseView(sp: SP): "list" | "map" {
  return one(sp.view) === "map" ? "map" : "list";
}
```

- [ ] **Step 2: Wire `/[category]/page.tsx`**

Add imports:
```ts
import { cachedCategoryBySlug, cachedDefinitionsByCategory, cachedListingList, cachedListingCountByCity } from "@/data/catalog/public-cached";
import { parseListParams, parseView } from "@/lib/catalog-search-params";
import { ListingMap } from "@/components/catalog/listing-map";
import { ViewToggle } from "@/components/catalog/view-toggle";
```

After `const result = await cachedListingList(input);` (line 66), add:
```ts
  const view = parseView(sp);
  const pins = view === "map" ? await cachedListingCountByCity(input) : [];
```

Replace the header actions cell — change `<CatalogSort sort={input.sort} />` (line 117) to:
```tsx
        <div className="flex items-center gap-2">
          <ViewToggle view={view} />
          <CatalogSort sort={input.sort} />
        </div>
```

Replace the results cell body (lines 124–144, the `<div className="min-w-0 flex-1">…</div>`) so map view swaps in:
```tsx
        <div className="min-w-0 flex-1">
          {view === "map" ? (
            <ListingMap pins={pins} mode="query" categorySlug={categorySlug} />
          ) : result.items.length > 0 ? (
            <>
              <ListingGrid listings={result.items} locale={locale} />
              <div className="mt-8">
                <CatalogPagination
                  total={result.total}
                  page={result.page}
                  perPage={result.perPage}
                  basePath={`/${categorySlug}`}
                  searchParams={sp}
                />
              </div>
            </>
          ) : (
            <div className="rounded-lg border border-border py-16 text-center">
              <p className="font-serif text-2xl">{t("empty")}</p>
              <p className="mt-2 text-sm text-muted-foreground">{t("emptyHint")}</p>
            </div>
          )}
        </div>
```

- [ ] **Step 3: Wire `/[category]/[city]/page.tsx`**

Add the same four imports (adjust the `public-cached` import to include `cachedListingCountByCity` alongside the existing named imports; add `parseView`, `ListingMap`, `ViewToggle`).

After `const result = await cachedListingList(input);` (line 74), add:
```ts
  const view = parseView(sp);
  // countByCity игнорира input.cityId → пиновете покриват всички градове на категорията;
  // текущият град се подсветва. Клик по пин навигира към geo-landing на друг град.
  const pins = view === "map" ? await cachedListingCountByCity(input) : [];
```

Change `<CatalogSort sort={input.sort} />` (line 141) to:
```tsx
        <div className="flex items-center gap-2">
          <ViewToggle view={view} />
          <CatalogSort sort={input.sort} />
        </div>
```

Replace the results cell (lines 148–168) so map view swaps in with route mode + active city:
```tsx
        <div className="min-w-0 flex-1">
          {view === "map" ? (
            <ListingMap pins={pins} mode="route" categorySlug={categorySlug} activeCitySlug={citySlug} />
          ) : result.items.length > 0 ? (
            <>
              <ListingGrid listings={result.items} locale={locale} />
              <div className="mt-8">
                <CatalogPagination
                  total={result.total}
                  page={result.page}
                  perPage={result.perPage}
                  basePath={`/${categorySlug}/${citySlug}`}
                  searchParams={sp}
                />
              </div>
            </>
          ) : (
            <div className="rounded-lg border border-border py-16 text-center">
              <p className="font-serif text-2xl">{t("empty")}</p>
              <p className="mt-2 text-sm text-muted-foreground">{t("emptyHint")}</p>
            </div>
          )}
        </div>
```

- [ ] **Step 4: Verify typecheck + build**

Run: `pnpm exec tsc --noEmit`
Expected: no errors.

Run: `pnpm build`
Expected: build succeeds (no SSR window errors).

- [ ] **Step 5: Dev smoke check**

Run `pnpm dev`, then verify:
- `/fotografi` renders the list; the toggle shows **Списък** active.
- `/fotografi?view=map` renders the Leaflet map with city pins + count badges; clicking a pin navigates to `/fotografi?city=<uuid>` (list of that city).
- `/fotografi/<city>` renders the geo-landing list; `?view=map` shows the map with all city pins and the current city highlighted; clicking another pin navigates to `/fotografi/<other-city>`.

- [ ] **Step 6: Commit**

```bash
git add src/lib/catalog-search-params.ts "src/app/[locale]/(public)/[category]/page.tsx" "src/app/[locale]/(public)/[category]/[city]/page.tsx"
git commit -m "feat(catalog): wire list/map toggle into catalog + geo-landing pages"
```

---

## Self-Review

- **Spec coverage:** toggle on both catalog pages ✅ (Task 6); city-level pins with count badge ✅ (Tasks 1+4); click→filter (`?city=uuid`) on category, click→route on geo-landing ✅ (Tasks 3+6); `wholeCountry` no pin ✅ (Task 1 `countByCity`); Leaflet+OSM client-only ✅ (Task 4); no CSP change needed (research §5). `/tarsene` map **deferred** by decision (2026-07-09) — not in scope.
- **Type consistency:** `PublicListingCityCount` defined in Task 1, consumed by name in Tasks 4+6; `PinLinkMode`/`cityPinHref` defined in Task 3, consumed in Task 4; `parseView` defined in Task 6 Step 1, used in both pages. `mode` prop is `"query" | "route"` everywhere (matches `PinLinkMode`).
- **Placeholder scan:** every step carries concrete code. Two verify-by-build tasks (4, 6) are flagged as such with the reason (Leaflet needs a real DOM).
- **Known follow-ups (not blocking):** map under-represents totals when whole-country listings exist for the category (they show in the list, not on the map) — this is the intended domain behavior; a "+N в цялата страна" affordance is a possible later polish. `fitBounds`-to-pins is skipped in favor of a fixed Bulgaria center/zoom (ponytail: add only if the fixed view proves too zoomed for edge cases).

## Execution Handoff

Plan complete. Recommended: **Subagent-Driven Development** — fresh implementer per task, task review between tasks, broad final review. Task 4 is the crux (SSR/dynamic import) → standard model; Tasks 1–3, 5 → cheap/standard; Task 6 → standard (multi-file wiring).
