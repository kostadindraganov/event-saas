# M2.1: Абонаменти (Polar) + публикационни лимити — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development

**Goal:** Абонаментен слой през Polar (checkout/portal/webhooks) с per-submit entitlement guard, който налага публикационни лимити (Standard = 1 обява общо, Premium = 2 на категория), 7-дневен гратис при просрочено плащане, системно скриване на обяви (grace-expiry/revoked/downgrade) с едно-кликово връщане и downgrade picker, 2 email-а и `/profil/dostavchik/abonament` UI.

**Architecture:** Entitlement е per-submit DAL guard (`BillingDAL.assertCanPublish`), викан вътре в submit()/unhide() транзакцията, НЕ blanket route gate — draft create/update остават свободни. Абонаментната проекция е единствен идемпотентен upsert path (`projectSubscriptionEvent`) от условния Polar plugin в `auth.ts`; системното скриване минава през обща `hiddenBySystem` колона, която ръчният hide/unhide не пипа. UI-ят чете през RSC→DAL (`billing.mine`) и мутира през tRPC (`restoreListings`/`keepListing`); checkout/portal се викат директно през `authClient`, извън tRPC.

**Tech Stack:** Next.js 16.2.10 (App Router, БЕЗ Cache Components), next-intl (bg без префикс, /en), Drizzle + Neon Postgres (neon-serverless Pool, `db.transaction`), tRPC v11 (`billing` router), `@polar-sh/better-auth@^1.8` + `@polar-sh/sdk` (условен plugin), string-builder email-и в `src/lib/email.ts` (без React Email), VPS crontab cron endpoint, vitest + dev-Neon интеграционни тестове, Playwright MCP за E2E.

## Global Constraints

Всяка задача имплицитно включва тези изисквания (копирани върбатим от договора):

- Next.js 16.2.10 App Router; `revalidateTag(tag, { expire: 0 })` (2-арг.); noUncheckedIndexedAccess → `row?.x ?? fallback`.
- tRPC v11 единствена клиентска граница; НИКАКВИ Server Actions. RSC чете DAL директно.
- Drizzle + neon-serverless Pool; PG error код при wrap: `err.cause.code`, не `err.code`.
- DAL/DTO/Policy; DTO не съдържат чужди id-та/email-и.
- Секрети САМО в `.env`; `.env.example` само празни placeholder-и. `git add` конкретни пътища, никога `-A`.
- i18n: bg/en byte-mirrored; навигация само през `@/i18n/navigation`.
- UX: изчистен/луксозен/минималистичен/mobile-first; stone + wine #9F1239 semantic tokens; Cormorant+Inter; touch ≥44px; един primary CTA на екран.
- Чат/резервации/съществуващи запитвания НЕ зависят от абонамента — hide спира само нови запитвания (вече е така: createInquiry изисква published; НЕ пипай messaging).
- `promotion` таблицата НЕ се пипа.

**Merge-ред зависимости (задължителни — не изпълнявай зависими задачи паралелно):**

- **Задача 5 разширява СЪЩИЯ `webhooks({...})` литерал в `src/lib/auth.ts`, който Задача 4 създава** — двете НЕ се изпълняват паралелно (Задача 5 merge-ва след Задача 4).
- **Задача 7 изисква Задача 6** (`notifyListingsHiddenEmail` е export от Задача 6, консумиран в `expireGracePeriods`).
- **Задача 3 изисква Задача 1** (`createTestSubscription`) **и Задача 2** (`BillingDAL.assertCanPublish` + `getBillingSettings` + `export type Transaction`).
- Задачи 5/6/7/8 всичките **разширяват** `src/data/billing/billing.dal.ts` (създаден от Задача 2) — не създават нов файл; изпълняват се последователно.
- Задача 8 добавя `private constructor` + `static for()` към `BillingDAL` (Задачи 2/5/7 добавят само static методи) — инстанс методите `mine/restoreListings/keepListing` зависят от това.
- Задачи 10–12 (UI) зависят от Задача 8 (DAL/DTO/router) и Задача 9 (i18n `Billing` namespace). Задача 10 импортира `DowngradeListingPicker` от Задача 11 → build проверката на Задача 10 се отлага до след Задача 11 (или Задача 11 предхожда).
- Задача 13 (E2E) — накрая, изисква завършени Задачи 1–12.

## Обхват

**IN:** Polar checkout/portal/webhooks през `@polar-sh/better-auth`; `subscription` проекция; entitlement при publish (submit + unhide); 7-дневен гратис cron → hidden; downgrade Premium→Standard picker; едно-кликово връщане; 2 email-а (past_due, listings hidden); `/profil/dostavchik/abonament` UI; publish-gate UI.

**OUT (изрично):** промотиране (M2.2), админ панел + pending_approval (M2.3), React Email, rate limiting, `promotion` таблицата (НЕ се пипа — промо прозорецът тече и при hidden, hide логиката НЕ докосва promotion редове).

---

# Задачи 1–3 (DB схема, entitlement DAL, CAS wiring)

> Backend-first, TDD. Всяка задача е самостоятелна: имплементаторът вижда САМО своята задача. Всеки код блок е ПЪЛНИЯТ код за файла/метода — копира се 1:1, не е псевдокод. Тестове срещу dev Neon, owner/id-scoped asserts.

---

### Задача 1: Схема `listing.hiddenBySystem` + миграция; тест хелпъри за subscription

**Files:**
- Modify: `src/db/schema/catalog.ts`
- Create: миграция чрез `drizzle-kit generate` → `drizzle/000N_<generated-name>.sql`
- Modify: `src/test/db-helpers.ts`
- Modify: `src/test/db-helpers.test.ts`

**Interfaces:**
- Consumes: нищо (основа за Задачи 2 и 3).
- Produces:
  - Колона `listing.hiddenBySystem: boolean` (notNull, default `false`).
  - `createTestSubscription(userId: string, opts: { plan: "standard" | "premium"; status: "active" | "past_due" | "canceled" | "revoked"; graceUntil?: Date | null }): Promise<typeof schema.subscription.$inferSelect>` в `src/test/db-helpers.ts` — Задача 2 и 3 я викат в тестовете си.
  - `cleanupTestUser(userId)` вече трие `subscription` реда на потребителя ПРЕДИ да трие `user` реда (FK).

- [ ] **Стъпка 1 — failing test** (`src/test/db-helpers.test.ts`, добави след съществуващия `test(...)` блок, в края на файла):

```ts
test("listing.hiddenBySystem: default false на нов draft", async () => {
  const u = await createTestUser();
  const categoryId = await getTestCategoryId();
  const cityId = await getTestCityId();
  const created = await ListingDAL.for({ id: u.id, email: u.email, name: "Т", isAdmin: false })
    .createDraft({ title: "Хидън Систем Дефолт Тест", categoryId, cityId });
  const [row] = await testDb.select().from(schema.listing).where(eq(schema.listing.id, created.id));
  expect(row?.hiddenBySystem).toBe(false);
  await cleanupTestUser(u.id);
});

test("createTestSubscription: вмъква ред; повторен извикване презаписва; cleanupTestUser го трие преди user — без FK грешка", async () => {
  const u = await createTestUser();
  const graceUntil = new Date(Date.now() + 86_400_000);
  const sub = await createTestSubscription(u.id, { plan: "premium", status: "past_due", graceUntil });
  expect(sub.userId).toBe(u.id);
  expect(sub.plan).toBe("premium");
  expect(sub.status).toBe("past_due");
  expect(sub.graceUntil?.getTime()).toBe(graceUntil.getTime());

  // повторно извикване (напр. друг тест сменя плана на същия owner) → delete-then-insert, не гърми на unique(userId)
  const sub2 = await createTestSubscription(u.id, { plan: "standard", status: "active" });
  expect(sub2.plan).toBe("standard");
  expect(
    (await testDb.select().from(schema.subscription).where(eq(schema.subscription.userId, u.id))).length,
  ).toBe(1);

  await cleanupTestUser(u.id);
  expect((await testDb.select().from(schema.user).where(eq(schema.user.id, u.id))).length).toBe(0);
  expect(
    (await testDb.select().from(schema.subscription).where(eq(schema.subscription.userId, u.id))).length,
  ).toBe(0);
});
```

Добави `createTestSubscription` към импорта в горната част на файла:
```ts
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCategoryId, getTestCityId, testDb } from "@/test/db-helpers";
```

- [ ] **Run (очаква се FAIL)**: `npx vitest run src/test/db-helpers.test.ts`
  Очакван резултат: и двата нови теста падат — `hiddenBySystem` е `undefined` (колоната не съществува в schema обекта), `createTestSubscription` не е exported (TS/import грешка).

- [ ] **Имплементация 1 — схема** (`src/db/schema/catalog.ts`): в `listing` таблицата добави полето веднага след `rejectionReason`:

Преди:
```ts
  status: listingStatus("status").notNull().default("draft"),
  rejectionReason: text("rejection_reason"),
  // кеширани агрегати (преизчисляват се при ново ревю / промяна на пакети)
```
След:
```ts
  status: listingStatus("status").notNull().default("draft"),
  rejectionReason: text("rejection_reason"),
  // системно скриване (гратис изтекъл/revoked/downgrade) — НЕ се пипа при ръчен hide/unhide; publish пътищата (submit/unhide) винаги я нулират
  hiddenBySystem: boolean("hidden_by_system").notNull().default(false),
  // кеширани агрегати (преизчисляват се при ново ревю / промяна на пакети)
```
(`boolean` вече е импортиран в горната част на файла — реекспорт от `drizzle-orm/pg-core`, виж L1-4.)

- [ ] **Имплементация 2 — миграция**: изпълни
```
npm run db:generate
```
Drizzle-kit чете diff-а между schema и последната миграция (`drizzle/0003_wide_green_goblin.sql` е последната) и генерира нов файл `drizzle/0004_<случайно-генерирано-име>.sql` (следващ пореден индекс 4, drizzle-kit сам избира adjective_noun име — запиши точното име, което получиш). Очакван SQL (единствен statement, по същия формат като `0002_lively_callisto.sql`):
```sql
ALTER TABLE "listing" ADD COLUMN "hidden_by_system" boolean DEFAULT false NOT NULL;
```
Приложи я към dev/test базата:
```
npm run db:migrate
```

- [ ] **Имплементация 3 — тест хелпъри** (`src/test/db-helpers.ts`): добави `createTestSubscription` след `createTestUser`, и разшири `cleanupTestUser` (замества съществуващите две функции, обхват L13-35):

```ts
export async function createTestUser() {
  const id = randomUUID();
  const email = `test-${id}@event-review.test`;
  await testDb.insert(schema.user).values({
    id,
    email,
    name: "Тест Потребител",
    emailVerified: false,
    createdAt: new Date(),
    updatedAt: new Date(),
  });
  return { id, email };
}

export async function createTestSubscription(
  userId: string,
  opts: {
    plan: "standard" | "premium";
    status: "active" | "past_due" | "canceled" | "revoked";
    graceUntil?: Date | null;
  },
) {
  // userId е unique — трий преди повторен insert (тест сменя план/статус на същия owner)
  await testDb.delete(schema.subscription).where(eq(schema.subscription.userId, userId));
  const [row] = await testDb
    .insert(schema.subscription)
    .values({
      userId,
      polarSubscriptionId: `test-${randomUUID()}`,
      plan: opts.plan,
      status: opts.status,
      graceUntil: opts.graceUntil ?? null,
    })
    .returning();
  if (!row) throw new Error("INSERT_FAILED");
  return row;
}

export async function cleanupTestUser(userId: string) {
  // thread FK-тата (customerId/vendorId) са no-action → трий нишките първо (message каскадира от thread)
  await testDb.delete(schema.thread).where(
    or(eq(schema.thread.vendorId, userId), eq(schema.thread.customerId, userId)),
  );
  // subscription.userId FK → user.id е no-action; трий преди user
  await testDb.delete(schema.subscription).where(eq(schema.subscription.userId, userId));
  // обявите каскадират децата си; savedListing.userId има onDelete cascade → авто при delete user
  await testDb.delete(schema.listing).where(eq(schema.listing.ownerId, userId));
  await testDb.delete(schema.user).where(eq(schema.user.id, userId));
}
```
(`getTestCategoryId`/`getTestCityId` остават непроменени, под тези функции; `randomUUID`, `eq`, `or`, `testDb`, `schema` вече са импортирани в горната част на файла.)

- [ ] **Run (очаква се PASS)**: `npx vitest run src/test/db-helpers.test.ts`

- [ ] **Commit**:
```
git add src/db/schema/catalog.ts drizzle/0004_*.sql drizzle/meta src/test/db-helpers.ts src/test/db-helpers.test.ts
git commit -m "db: add listing.hiddenBySystem column + test subscription helpers"
```
(замести `0004_*.sql` с точното генерирано име при `git add` — точен path, без wildcard/`-A` в реалния repo).

---

### Задача 2: `getBillingSettings()` + seed на `setting` + `BillingDAL.assertCanPublish` (+ count helper)

**Files:**
- Create: `src/data/billing/billing.dal.ts`
- Create: `src/data/billing/billing.dal.test.ts`
- Modify: `scripts/seed.ts`

**Interfaces:**
- Consumes (от Задача 1): `createTestSubscription(userId, opts)`, `cleanupTestUser`, `getTestCityId` от `src/test/db-helpers.ts`; колоната `listing.hiddenBySystem` (не се ползва пряко тук, само наследство от схемата).
- Produces (за Задача 3 и нататък):
  - `export type Transaction = Parameters<Parameters<typeof db.transaction>[0]>[0];` от `src/data/billing/billing.dal.ts` — типът на `tx` вътре в `db.transaction(async (tx) => ...)`. **Каноничното име на tx типа е `Transaction`** — Задачи 5/7/8 го преизползват, не декларират нов алиас.
  - `export class BillingDAL { static async assertCanPublish(tx: Transaction, userId: string, categoryId: string, excludeListingId: string): Promise<void> }` — **static метод на класа** (вика се като `BillingDAL.assertCanPublish(...)`); хвърля `TRPCError({code:"FORBIDDEN", message:"NO_SUBSCRIPTION"})` или `TRPCError({code:"FORBIDDEN", message:"LIMIT_REACHED"})`; при успех `void`.
  - `export async function getBillingSettings(): Promise<{ limits: { standard: number; premiumPerCategory: number }; graceDays: number }>` — **standalone export** (НЕ метод на класа); defaults `{standard:1, premiumPerCategory:2}` / `7` ако `setting` редовете липсват.

- [ ] **Стъпка 1 — failing test** (нов файл `src/data/billing/billing.dal.test.ts`):

```ts
import { afterEach, expect, test } from "vitest";
import { eq } from "drizzle-orm";
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCityId, testDb } from "@/test/db-helpers";
import * as schema from "@/db/schema";
import { ListingDAL } from "@/data/catalog/listing.dal";
import type { SessionUser } from "@/data/users/require-user";
import { BillingDAL, getBillingSettings } from "./billing.dal";

let cleanupIds: string[] = [];
afterEach(async () => {
  for (const id of cleanupIds) await cleanupTestUser(id);
  cleanupIds = [];
});

async function newOwner(): Promise<{ user: SessionUser; id: string }> {
  const u = await createTestUser();
  cleanupIds.push(u.id);
  return { user: { id: u.id, email: u.email, name: "Тест", isAdmin: false }, id: u.id };
}

async function twoCategories(): Promise<[string, string]> {
  const cats = await testDb
    .select({ id: schema.category.id })
    .from(schema.category)
    .orderBy(schema.category.slug)
    .limit(2);
  if (cats.length < 2) throw new Error("seed липсва: нужни ≥2 категории (npm run db:seed)");
  return [cats[0]!.id, cats[1]!.id];
}

async function publishedListing(user: SessionUser, categoryId: string, cityId: string, title: string): Promise<string> {
  const draft = await ListingDAL.for(user).createDraft({ title, categoryId, cityId });
  await testDb.update(schema.listing).set({ status: "published", publishedAt: new Date() }).where(eq(schema.listing.id, draft.id));
  return draft.id;
}

function checkPublish(userId: string, categoryId: string, excludeId: string) {
  return testDb.transaction((tx) => BillingDAL.assertCanPublish(tx, userId, categoryId, excludeId));
}

test("getBillingSettings(): връща валидни (seed или code-side default) граници", async () => {
  const s = await getBillingSettings();
  expect(s.limits.standard).toBeGreaterThanOrEqual(1);
  expect(s.limits.premiumPerCategory).toBeGreaterThanOrEqual(1);
  expect(s.graceDays).toBeGreaterThanOrEqual(1);
});

test("без subscription ред → NO_SUBSCRIPTION", async () => {
  const { user, id } = await newOwner();
  const cityId = await getTestCityId();
  const [categoryA] = await twoCategories();
  const draft = await ListingDAL.for(user).createDraft({ title: "Ентайтълмънт Без Абонамент", categoryId: categoryA!, cityId });
  await expect(checkPublish(id, categoryA!, draft.id)).rejects.toMatchObject({ code: "FORBIDDEN", message: "NO_SUBSCRIPTION" });
});

test("standard с 1 published → LIMIT_REACHED на втора; собствената обява е excluded от броенето", async () => {
  const { user, id } = await newOwner();
  const cityId = await getTestCityId();
  const [categoryA] = await twoCategories();
  await createTestSubscription(id, { plan: "standard", status: "active" });
  const first = await publishedListing(user, categoryA!, cityId, "Стандарт Публикувана");
  const second = await ListingDAL.for(user).createDraft({ title: "Стандарт Втора", categoryId: categoryA!, cityId });
  await expect(checkPublish(id, categoryA!, second.id)).rejects.toMatchObject({ code: "FORBIDDEN", message: "LIMIT_REACHED" });
  // excludeListingId изключва самата вече публикувана обява от собственото ѝ броене
  await expect(checkPublish(id, categoryA!, first)).resolves.toBeUndefined();
});

test("premium: 2 в категория ОК, 3-та в същата категория → LIMIT_REACHED, друга категория минава", async () => {
  const { user, id } = await newOwner();
  const cityId = await getTestCityId();
  const [categoryA, categoryB] = await twoCategories();
  await createTestSubscription(id, { plan: "premium", status: "active" });
  await publishedListing(user, categoryA!, cityId, "Премиум 1");
  await publishedListing(user, categoryA!, cityId, "Премиум 2");
  const third = await ListingDAL.for(user).createDraft({ title: "Премиум 3", categoryId: categoryA!, cityId });
  await expect(checkPublish(id, categoryA!, third.id)).rejects.toMatchObject({ code: "FORBIDDEN", message: "LIMIT_REACHED" });
  const otherCategory = await ListingDAL.for(user).createDraft({ title: "Премиум Друга Категория", categoryId: categoryB!, cityId });
  await expect(checkPublish(id, categoryB!, otherCategory.id)).resolves.toBeUndefined();
});

test("past_due в гратис → минава; изтекъл гратис → NO_SUBSCRIPTION", async () => {
  const { user, id } = await newOwner();
  const cityId = await getTestCityId();
  const [categoryA] = await twoCategories();
  const future = new Date(Date.now() + 24 * 60 * 60 * 1000);
  await createTestSubscription(id, { plan: "standard", status: "past_due", graceUntil: future });
  const a = await ListingDAL.for(user).createDraft({ title: "Гратис Тест", categoryId: categoryA!, cityId });
  await expect(checkPublish(id, categoryA!, a.id)).resolves.toBeUndefined();

  const past = new Date(Date.now() - 24 * 60 * 60 * 1000);
  await createTestSubscription(id, { plan: "standard", status: "past_due", graceUntil: past });
  const b = await ListingDAL.for(user).createDraft({ title: "Изтекъл Гратис Тест", categoryId: categoryA!, cityId });
  await expect(checkPublish(id, categoryA!, b.id)).rejects.toMatchObject({ code: "FORBIDDEN", message: "NO_SUBSCRIPTION" });
});
```

- [ ] **Run (очаква се FAIL)**: `npx vitest run src/data/billing/billing.dal.test.ts`
  Очакван резултат: import грешка — `./billing.dal` не съществува.

- [ ] **Имплементация** (нов файл `src/data/billing/billing.dal.ts`):

```ts
import "server-only";
import { and, count, eq, inArray, ne } from "drizzle-orm";
import { TRPCError } from "@trpc/server";
import { db } from "@/db";
import { listing, setting, subscription } from "@/db/schema";

// типът на tx вътре в db.transaction(async (tx) => ...) — преизползва се от submit()/unhide() (Задача 3)
// и от разширенията в Задачи 5/7/8. Каноничното име е `Transaction`.
export type Transaction = Parameters<Parameters<typeof db.transaction>[0]>[0];

export type BillingSettings = {
  limits: { standard: number; premiumPerCategory: number };
  graceDays: number;
};

const DEFAULT_SETTINGS: BillingSettings = {
  limits: { standard: 1, premiumPerCategory: 2 },
  graceDays: 7,
};

// ponytail: четено извън транзакцията — лимитите почти никога не се сменят,
// консистентност с count-а в submit()/unhide() транзакцията не носи полза тук.
export async function getBillingSettings(): Promise<BillingSettings> {
  const rows = await db
    .select()
    .from(setting)
    .where(inArray(setting.key, ["billing.limits", "billing.graceDays"]));
  const limits = rows.find((r) => r.key === "billing.limits")?.value as BillingSettings["limits"] | undefined;
  const graceDays = rows.find((r) => r.key === "billing.graceDays")?.value as number | undefined;
  return {
    limits: limits ?? DEFAULT_SETTINGS.limits,
    graceDays: graceDays ?? DEFAULT_SETTINGS.graceDays,
  };
}

async function countOwnerPublished(
  tx: Transaction,
  userId: string,
  excludeListingId: string,
  categoryId?: string,
): Promise<number> {
  const conditions = [
    eq(listing.ownerId, userId),
    eq(listing.status, "published"),
    ne(listing.id, excludeListingId),
  ];
  if (categoryId) conditions.push(eq(listing.categoryId, categoryId));
  const [row] = await tx.select({ n: count() }).from(listing).where(and(...conditions));
  return row?.n ?? 0;
}

export class BillingDAL {
  // Вика се вътре в submit()/unhide() транзакцията (tx), ПРЕДИ CAS UPDATE-а на listing.status.
  // Standard = общо 1 published (без значение от categoryId); Premium = 2 per categoryId.
  // excludeListingId маха обявата, която тъкмо се публикува, от собственото ѝ броене.
  static async assertCanPublish(
    tx: Transaction,
    userId: string,
    categoryId: string,
    excludeListingId: string,
  ): Promise<void> {
    const [sub] = await tx.select().from(subscription).where(eq(subscription.userId, userId));
    const now = Date.now();
    const active = !!sub && (
      sub.status === "active" ||
      (sub.status === "past_due" && !!sub.graceUntil && sub.graceUntil.getTime() > now)
    );
    if (!sub || !active) throw new TRPCError({ code: "FORBIDDEN", message: "NO_SUBSCRIPTION" });

    const { limits } = await getBillingSettings();
    const n = sub.plan === "standard"
      ? await countOwnerPublished(tx, userId, excludeListingId)
      : await countOwnerPublished(tx, userId, excludeListingId, categoryId);
    const limit = sub.plan === "standard" ? limits.standard : limits.premiumPerCategory;
    if (n >= limit) throw new TRPCError({ code: "FORBIDDEN", message: "LIMIT_REACHED" });
  }
}
```

- [ ] **Run (очаква се PASS)**: `npx vitest run src/data/billing/billing.dal.test.ts`

- [ ] **Имплементация — seed на `setting`** (`scripts/seed.ts`): добавя реалните default редове в dev/prod базата (не е нужен за да минат тестовете по-горе — `getBillingSettings()` дефолтва code-side — но е нужен, за да е лимитът конфигурируем без redeploy).

Добави към импортите:
```ts
import { setting } from "../src/db/schema/billing";
```
Добави в `main()`, преди `await seedAttributes();`:
```ts
  await db
    .insert(setting)
    .values([
      { key: "billing.limits", value: { standard: 1, premiumPerCategory: 2 } },
      { key: "billing.graceDays", value: 7 },
    ])
    .onConflictDoNothing({ target: setting.key });
```

- [ ] **Run**: `npm run db:seed` (идемпотентно при повторно изпълнение заради `onConflictDoNothing`; успех = `"seed done"` в конзолата, без грешка).

- [ ] **Commit**:
```
git add src/data/billing/billing.dal.ts src/data/billing/billing.dal.test.ts scripts/seed.ts
git commit -m "billing: add getBillingSettings + assertCanPublish entitlement guard"
```

---

### Задача 3: CAS транзакция в `submit()`/`unhide()` + entitlement wiring

**Files:**
- Modify: `src/data/catalog/listing.dal.ts`
- Modify: `src/data/catalog/listing.dal.test.ts` (даваш на `owner` subscription, за да продължат да минават съществуващите submit/hide/unhide тестове)
- Create: `src/data/catalog/listing-entitlement.dal.test.ts`

**Interfaces:**
- Consumes (от Задача 2): `BillingDAL.assertCanPublish(tx, userId, categoryId, excludeListingId)` от `@/data/billing/billing.dal` (static метод; хвърля `TRPCError` с `code:"FORBIDDEN"`, `message: "NO_SUBSCRIPTION" | "LIMIT_REACHED"`).
- Consumes (от Задача 1): `createTestSubscription`, `cleanupTestUser` от `@/test/db-helpers`; колоната `listing.hiddenBySystem`.
- Produces: `submit()`/`unhide()` запазват публичните си сигнатури (`(id: string): Promise<ListingDTO>`); поведението при валиден план е непроменено; при невалиден план хвърлят `TRPCError` (NO_SUBSCRIPTION/LIMIT_REACHED) вместо да публикуват. И двете сега сетват `hiddenBySystem: false` при успешна публикация. `hide()` е непроменен (не пипа `hiddenBySystem`, не изисква entitlement).

- [ ] **Стъпка 1a — модификация на съществуващ тест setup**, за да продължат да минават текущите submit/hide/unhide тестове (`src/data/catalog/listing.dal.test.ts`): подмени импорт реда и добави `createTestSubscription` в `beforeAll`:

Преди:
```ts
import { createTestUser, cleanupTestUser, getTestCategoryId, getTestCityId } from "@/test/db-helpers";
```
```ts
beforeAll(async () => {
  const u1 = await createTestUser();
  const u2 = await createTestUser();
  ownerId = u1.id;
  strangerId = u2.id;
  owner = { id: u1.id, email: u1.email, name: "Тест", isAdmin: false };
  stranger = { id: u2.id, email: u2.email, name: "Друг", isAdmin: false };
  categoryId = await getTestCategoryId();
  cityId = await getTestCityId();
});
```
След:
```ts
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCategoryId, getTestCityId } from "@/test/db-helpers";
```
```ts
beforeAll(async () => {
  const u1 = await createTestUser();
  const u2 = await createTestUser();
  ownerId = u1.id;
  strangerId = u2.id;
  owner = { id: u1.id, email: u1.email, name: "Тест", isAdmin: false };
  stranger = { id: u2.id, email: u2.email, name: "Друг", isAdmin: false };
  categoryId = await getTestCategoryId();
  cityId = await getTestCityId();
  // submit()/unhide() вече изискват активен план (Задача 3) — този файл публикува точно 1 обява за owner → standard стига
  await createTestSubscription(ownerId, { plan: "standard", status: "active" });
});
```

- [ ] **Стъпка 1b — failing test** (нов файл `src/data/catalog/listing-entitlement.dal.test.ts`):

```ts
import { afterEach, expect, test } from "vitest";
import { eq } from "drizzle-orm";
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCategoryId, getTestCityId, testDb } from "@/test/db-helpers";
import * as schema from "@/db/schema";
import { ListingDAL } from "./listing.dal";
import type { SessionUser } from "@/data/users/require-user";

let cleanupIds: string[] = [];
afterEach(async () => {
  for (const id of cleanupIds) await cleanupTestUser(id);
  cleanupIds = [];
});

async function newOwner(): Promise<{ user: SessionUser; id: string }> {
  const u = await createTestUser();
  cleanupIds.push(u.id);
  return { user: { id: u.id, email: u.email, name: "Тест", isAdmin: false }, id: u.id };
}

test("submit(): без subscription → NO_SUBSCRIPTION", async () => {
  const { user } = await newOwner();
  const categoryId = await getTestCategoryId();
  const cityId = await getTestCityId();
  const dal = ListingDAL.for(user);
  const draft = await dal.createDraft({ title: "Ентайтълмънт Submit Тест", categoryId, cityId });
  await expect(dal.submit(draft.id)).rejects.toThrow("NO_SUBSCRIPTION");
});

test("submit(): standard с 1 published → LIMIT_REACHED на втора; hide→unhide в лимит минава и hiddenBySystem остава false", async () => {
  const { user, id } = await newOwner();
  const categoryId = await getTestCategoryId();
  const cityId = await getTestCityId();
  await createTestSubscription(id, { plan: "standard", status: "active" });
  const dal = ListingDAL.for(user);

  const first = await dal.createDraft({ title: "Лимит Първа", categoryId, cityId });
  await dal.submit(first.id);

  const second = await dal.createDraft({ title: "Лимит Втора", categoryId, cityId });
  await expect(dal.submit(second.id)).rejects.toThrow("LIMIT_REACHED");

  // ръчен hide освобождава лимита; unhide (в лимит) минава; hiddenBySystem не се пипа от ръчния hide → остава false
  await dal.hide(first.id);
  const unhidden = await dal.unhide(first.id);
  expect(unhidden.status).toBe("published");
  const [row] = await testDb.select().from(schema.listing).where(eq(schema.listing.id, first.id));
  expect(row?.hiddenBySystem).toBe(false);
});
```

- [ ] **Run (очаква се FAIL)**: `npx vitest run src/data/catalog/listing-entitlement.dal.test.ts src/data/catalog/listing.dal.test.ts`
  Очакван резултат: `listing-entitlement.dal.test.ts` пада (submit()/unhide() публикуват без проверка на план); `listing.dal.test.ts` минава и преди промяната (потвърждава baseline, преди да се разчита на новото `beforeAll`).

- [ ] **Имплементация** (`src/data/catalog/listing.dal.ts`):

Импортите в началото на файла — преди:
```ts
import "server-only";
import { and, eq } from "drizzle-orm";
import { db } from "@/db";
import { listing, listingServiceRegion } from "@/db/schema";
import { slugifyBg } from "@/lib/slug";
import type { SessionUser } from "@/data/users/require-user";
import { canCreateListing, canEditListing, canSubmitListing } from "./catalog.policy";
import type { ListingCreateInput, ListingDTO, ListingSummaryDTO, ListingUpdateInput } from "./catalog.dto";
import { PublicListingDAL } from "./public-listing.dal";
```
след:
```ts
import "server-only";
import { and, eq, or } from "drizzle-orm";
import { db } from "@/db";
import { listing, listingServiceRegion } from "@/db/schema";
import { slugifyBg } from "@/lib/slug";
import type { SessionUser } from "@/data/users/require-user";
import { canCreateListing, canEditListing, canSubmitListing } from "./catalog.policy";
import type { ListingCreateInput, ListingDTO, ListingSummaryDTO, ListingUpdateInput } from "./catalog.dto";
import { PublicListingDAL } from "./public-listing.dal";
import { BillingDAL } from "@/data/billing/billing.dal";
```

`submit()` — преди:
```ts
  async submit(id: string): Promise<ListingDTO> {
    const row = await this.ownedRow(id);
    if (!canSubmitListing(this.user, row)) throw new Error("FORBIDDEN");
    // ponytail: Ф1 публикува директно; pending_approval gate идва с админ панела (Ф2)
    const [updated] = await db
      .update(listing)
      .set({ status: "published", publishedAt: new Date(), rejectionReason: null, updatedAt: new Date() })
      .where(eq(listing.id, id))
      .returning();
    if (!updated) throw new Error("NOT_FOUND");
    return toDTO(updated, await this.regionIds(id));
  }
```
след:
```ts
  async submit(id: string): Promise<ListingDTO> {
    const row = await this.ownedRow(id);
    if (!canSubmitListing(this.user, row)) throw new Error("FORBIDDEN");
    const userId = this.user.id;
    // ponytail: Ф1 публикува директно; pending_approval gate идва с админ панела (Ф2)
    // транзакция: entitlement guard + CAS UPDATE атомарно (затваря TOCTOU при конкурентен submit)
    const updated = await db.transaction(async (tx) => {
      const [fresh] = await tx
        .select({ categoryId: listing.categoryId, status: listing.status })
        .from(listing)
        .where(eq(listing.id, id));
      if (!fresh || (fresh.status !== "draft" && fresh.status !== "rejected")) throw new Error("FORBIDDEN");
      await BillingDAL.assertCanPublish(tx, userId, fresh.categoryId, id);
      const [updatedRow] = await tx
        .update(listing)
        .set({
          status: "published",
          publishedAt: new Date(),
          rejectionReason: null,
          hiddenBySystem: false,
          updatedAt: new Date(),
        })
        .where(and(eq(listing.id, id), or(eq(listing.status, "draft"), eq(listing.status, "rejected"))))
        .returning();
      if (!updatedRow) throw new Error("FORBIDDEN"); // CAS изгубена — статусът се е сменил конкурентно
      return updatedRow;
    });
    return toDTO(updated, await this.regionIds(id));
  }
```

`setStatus`/`hide`/`unhide` — преди:
```ts
  private async setStatus(id: string, from: "published" | "hidden", to: "published" | "hidden"): Promise<ListingDTO> {
    const row = await this.ownedRow(id);
    if (row.status !== from) throw new Error("FORBIDDEN");
    const [updated] = await db
      .update(listing)
      .set({ status: to, updatedAt: new Date() })
      .where(and(eq(listing.id, id), eq(listing.status, from)))
      .returning();
    if (!updated) throw new Error("NOT_FOUND");
    return toDTO(updated, await this.regionIds(id));
  }

  hide(id: string) {
    return this.setStatus(id, "published", "hidden");
  }

  unhide(id: string) {
    return this.setStatus(id, "hidden", "published");
  }
```
след (`setStatus`/`hide` НЕПРОМЕНЕНИ — обслужват само `hide()`, което не изисква entitlement; `unhide()` вече е самостоятелен метод с транзакция, вместо да делегира на `setStatus`):
```ts
  private async setStatus(id: string, from: "published" | "hidden", to: "published" | "hidden"): Promise<ListingDTO> {
    const row = await this.ownedRow(id);
    if (row.status !== from) throw new Error("FORBIDDEN");
    const [updated] = await db
      .update(listing)
      .set({ status: to, updatedAt: new Date() })
      .where(and(eq(listing.id, id), eq(listing.status, from)))
      .returning();
    if (!updated) throw new Error("NOT_FOUND");
    return toDTO(updated, await this.regionIds(id));
  }

  hide(id: string) {
    return this.setStatus(id, "published", "hidden");
  }

  async unhide(id: string): Promise<ListingDTO> {
    const row = await this.ownedRow(id);
    if (row.status !== "hidden") throw new Error("FORBIDDEN");
    const userId = this.user.id;
    // транзакция: entitlement guard + CAS UPDATE атомарно (иначе hide→unhide заобикаля лимита от submit())
    const updated = await db.transaction(async (tx) => {
      const [fresh] = await tx
        .select({ categoryId: listing.categoryId, status: listing.status })
        .from(listing)
        .where(eq(listing.id, id));
      if (!fresh || fresh.status !== "hidden") throw new Error("FORBIDDEN");
      await BillingDAL.assertCanPublish(tx, userId, fresh.categoryId, id);
      const [updatedRow] = await tx
        .update(listing)
        .set({ status: "published", hiddenBySystem: false, updatedAt: new Date() })
        .where(and(eq(listing.id, id), eq(listing.status, "hidden")))
        .returning();
      if (!updatedRow) throw new Error("FORBIDDEN"); // CAS изгубена
      return updatedRow;
    });
    return toDTO(updated, await this.regionIds(id));
  }
```

- [ ] **Run (очаква се PASS)**: `npx vitest run src/data/catalog/listing-entitlement.dal.test.ts src/data/catalog/listing.dal.test.ts src/data/billing/billing.dal.test.ts src/test/db-helpers.test.ts`

- [ ] **Commit**:
```
git add src/data/catalog/listing.dal.ts src/data/catalog/listing.dal.test.ts src/data/catalog/listing-entitlement.dal.test.ts
git commit -m "catalog: wire entitlement guard + CAS transaction into submit/unhide"
```

---

# Задачи 4–7 (Polar wiring, webhook проекция, email-и, cron)

> Задачи 2/3 създадоха `src/data/billing/billing.dal.ts` (с `BillingDAL`, `getBillingSettings()`, `assertCanPublish`, `export type Transaction`) и `billing.dal.test.ts` — задачи 5/6/7 тук **разширяват** същия файл (static методи + helper-и), не създават нов. Задача 1 добави `listing.hiddenBySystem` колоната и `createTestSubscription`/`cleanupTestUser` разширение — задачи 5/7 ги ползват (Consumes).

---

### Задача 4: Polar пакети + условен плъгин (auth.ts / auth-client.ts)

**Files:**
- Modify: `package.json` (нови dependencies)
- Modify: `src/lib/auth.ts`
- Modify: `src/lib/auth-client.ts`
- Modify: `.env.example` (само празни placeholder-и — НИКАКВИ реални стойности)
- Test: `src/lib/auth.test.ts` (нов файл)

**Interfaces:**
- Consumes: `process.env.POLAR_ACCESS_TOKEN`, `POLAR_ENV`, `POLAR_WEBHOOK_SECRET`, `POLAR_PRODUCT_STANDARD_MONTHLY/STANDARD_YEARLY/PREMIUM_MONTHLY/PREMIUM_YEARLY`.
- Produces: `auth` (export, разширен с условен `polar()` плъгин, `webhooks()` **без** handler-и — Задача 5 ги добавя); `authClient` (разширен с `polarClient()`).

**Решение:** условието за плъгина е **само** `!!process.env.POLAR_ACCESS_TOKEN` (същия pattern като `hasGoogle`, без частична env проверка на 4-те продукта — ако липсват, checkout ще предаде `undefined` productId и Polar ще върне грешка при реален checkout опит, приемлив риск за V1, отбелязан по-долу). `webhooks({ secret })` се добавя тук БЕЗ handler callback-и — Задача 5 разширява същия `webhooks({...})` литерал, добавяйки `onSubscriptionActive/Updated/Canceled/Revoked`.

- [ ] **failing test** — `src/lib/auth.test.ts`:
```ts
import { afterEach, beforeEach, expect, test, vi } from "vitest";

// ponytail: стъпваме върху реалната .env (DATABASE_URL и др.) — само Polar ключовете
// се stub-ват, за да изолираме двата клона (с/без Polar) от каквото реално стои в .env.
const POLAR_ENV_KEYS = [
  "POLAR_ACCESS_TOKEN",
  "POLAR_WEBHOOK_SECRET",
  "POLAR_ENV",
  "POLAR_PRODUCT_STANDARD_MONTHLY",
  "POLAR_PRODUCT_STANDARD_YEARLY",
  "POLAR_PRODUCT_PREMIUM_MONTHLY",
  "POLAR_PRODUCT_PREMIUM_YEARLY",
] as const;

beforeEach(() => {
  vi.resetModules();
});

afterEach(() => {
  vi.unstubAllEnvs();
});

test("auth инициализира без грешка БЕЗ Polar env ключове", async () => {
  for (const k of POLAR_ENV_KEYS) vi.stubEnv(k, "");
  const { auth } = await import("./auth");
  expect(typeof auth.api.getSession).toBe("function");
});

test("auth инициализира без грешка С Polar env ключове", async () => {
  vi.stubEnv("POLAR_ACCESS_TOKEN", "polar_oat_test");
  vi.stubEnv("POLAR_WEBHOOK_SECRET", "whsec_test");
  vi.stubEnv("POLAR_ENV", "sandbox");
  vi.stubEnv("POLAR_PRODUCT_STANDARD_MONTHLY", "prod_standard_monthly");
  vi.stubEnv("POLAR_PRODUCT_STANDARD_YEARLY", "prod_standard_yearly");
  vi.stubEnv("POLAR_PRODUCT_PREMIUM_MONTHLY", "prod_premium_monthly");
  vi.stubEnv("POLAR_PRODUCT_PREMIUM_YEARLY", "prod_premium_yearly");
  const { auth } = await import("./auth");
  expect(typeof auth.api.getSession).toBe("function");
});
```
- [ ] **run (очаква се FAIL — модулите `@polar-sh/*` не съществуват, import ще гръмне):**
```bash
pnpm vitest run src/lib/auth.test.ts
```
- [ ] **имплементация** — пакети:
```bash
pnpm add @polar-sh/better-auth @polar-sh/sdk
```
- [ ] **имплементация** — `.env.example` (добавя се под съществуващия Polar блок, L24-27):
```
# ── Polar продукти (productId от Polar dashboard, sandbox или production) ── НУЖНО ОТ Task 4
POLAR_PRODUCT_STANDARD_MONTHLY=
POLAR_PRODUCT_STANDARD_YEARLY=
POLAR_PRODUCT_PREMIUM_MONTHLY=
POLAR_PRODUCT_PREMIUM_YEARLY=
```
- [ ] **имплементация** — `src/lib/auth.ts` (пълен нов файл):
```ts
import { betterAuth } from "better-auth";
import { drizzleAdapter } from "better-auth/adapters/drizzle";
import { nextCookies } from "better-auth/next-js";
import { polar, checkout, portal, webhooks } from "@polar-sh/better-auth";
import { Polar } from "@polar-sh/sdk";
import { db } from "@/db";

const hasGoogle = !!(process.env.GOOGLE_CLIENT_ID && process.env.GOOGLE_CLIENT_SECRET);

// ponytail: само POLAR_ACCESS_TOKEN гейтва плъгина (като hasGoogle) — ако 4-те
// POLAR_PRODUCT_* липсват, checkout() ще получи undefined productId; приемлив риск
// за local/dev без завършен Polar sandbox setup, ще гръмне ясно при реален checkout.
const hasPolar = !!process.env.POLAR_ACCESS_TOKEN;

const polarClient = hasPolar
  ? new Polar({
      accessToken: process.env.POLAR_ACCESS_TOKEN!,
      server: (process.env.POLAR_ENV as "sandbox" | "production" | undefined) ?? "sandbox",
    })
  : null;

export const auth = betterAuth({
  database: drizzleAdapter(db, { provider: "pg" }),
  emailAndPassword: { enabled: true },
  ...(hasGoogle
    ? {
        socialProviders: {
          google: {
            clientId: process.env.GOOGLE_CLIENT_ID!,
            clientSecret: process.env.GOOGLE_CLIENT_SECRET!,
          },
        },
      }
    : {}),
  user: {
    additionalFields: {
      isAdmin: { type: "boolean", defaultValue: false, input: false },
      phone: { type: "string", required: false },
    },
  },
  plugins: [
    ...(hasPolar && polarClient
      ? [
          polar({
            client: polarClient,
            createCustomerOnSignUp: true,
            use: [
              checkout({
                products: [
                  { productId: process.env.POLAR_PRODUCT_STANDARD_MONTHLY!, slug: "standard-monthly" },
                  { productId: process.env.POLAR_PRODUCT_STANDARD_YEARLY!, slug: "standard-yearly" },
                  { productId: process.env.POLAR_PRODUCT_PREMIUM_MONTHLY!, slug: "premium-monthly" },
                  { productId: process.env.POLAR_PRODUCT_PREMIUM_YEARLY!, slug: "premium-yearly" },
                ],
                successUrl: "/profil/dostavchik/abonament?checkout_id={CHECKOUT_ID}",
                authenticatedUsersOnly: true,
              }),
              portal(),
              // Задача 5 добавя onSubscriptionActive/Updated/Canceled/Revoked тук.
              webhooks({
                secret: process.env.POLAR_WEBHOOK_SECRET!,
              }),
            ],
          }),
        ]
      : []),
    nextCookies(),
  ],
});
```
- [ ] **имплементация** — `src/lib/auth-client.ts` (пълен нов файл):
```ts
import { createAuthClient } from "better-auth/react";
import { inferAdditionalFields } from "better-auth/client/plugins";
import { polarClient } from "@polar-sh/better-auth";
import type { auth } from "@/lib/auth";

export const authClient = createAuthClient({
  plugins: [inferAdditionalFields<typeof auth>(), polarClient()],
});
```
- [ ] **run pass:**
```bash
pnpm vitest run src/lib/auth.test.ts
```
- [ ] **commit:**
```bash
git add package.json pnpm-lock.yaml src/lib/auth.ts src/lib/auth-client.ts src/lib/auth.test.ts .env.example
git commit -m "billing: условен Polar плъгин (checkout/portal/webhooks) в auth.ts"
```

---

### Задача 5: `projectSubscriptionEvent` + webhook handlers + upsert + downgrade hide

**Files:**
- Modify: `src/lib/auth.ts` (добавя handler callback-и в съществуващия `webhooks({...})` от Задача 4)
- Modify: `src/data/billing/billing.dal.ts` (създаден от Задача 2/3 — добавя static методи + helper)
- Modify/Create: `src/data/billing/billing.dal.test.ts`

**Interfaces:**
- Consumes: `getBillingSettings(): Promise<{ limits: {...}; graceDays: number }>` (Задача 2, същия файл); `export type Transaction` (Задача 2, същия файл); `db`, `subscription`, `listing`, `user` schema; `createTestUser`/`cleanupTestUser`/`createTestSubscription` (Задача 1, `src/test/db-helpers.ts`).
- Produces: `BillingDAL.projectSubscriptionEvent(payload: PolarSubscriptionEventPayload): Promise<void>` (static); `hideAllPublished(tx: Transaction, userId: string): Promise<number>` (export, ползва се и от Задача 7); тип `PolarSubscriptionEventPayload` (export).

**Решение:** точната TS форма на Polar webhook payload-а не е потвърдена (плъгинът не е бил инсталиран/тестван реално) — дефинирам тесен локален тип с само нужните полета (`customer.externalId`, `data.status`, `data.id`, `data.currentPeriodEnd`, `data.productId`) и парсвам defensively: `mapStatus/mapPlan` връщат `null` при непознати стойности вместо да хвърлят, за да не строши webhook route-а на Better Auth при неочакван shape. Handler-ите в `auth.ts` увиват извикването в `try/catch` + `console.error` — Polar получава 200 винаги (webhook grief loop приемлив риск, отбелязан в contract т.8 — out-of-order/дублирана доставка е ОК заради idempotent upsert). Мапинг plan: productId срещу 4-те `POLAR_PRODUCT_*` env стойности. `graceUntil` set-once (само ако е бил `null` преди прехода към `past_due`); при `active` се чисти на `null`. Downgrade premium→standard: ако **преди** проекцията текущият ред е бил `premium` и новият `plan` е `standard`, и потребителят има >1 published обява → скрий всички (`hideAllPublished`). Revoked → винаги скрий всички published.

- [ ] **failing test** — добавка в `src/data/billing/billing.dal.test.ts`:
```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { listing, subscription } from "@/db/schema";
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCategoryId, getTestCityId } from "@/test/db-helpers";
import { BillingDAL } from "./billing.dal";
import { ListingDAL } from "@/data/catalog/listing.dal";
import type { SessionUser } from "@/data/users/require-user";

let userId: string;
let sessionUser: SessionUser;
let categoryId: string;
let cityId: string;

beforeAll(async () => {
  const u = await createTestUser();
  userId = u.id;
  sessionUser = { id: u.id, email: u.email, name: "Тест", isAdmin: false };
  categoryId = await getTestCategoryId();
  cityId = await getTestCityId();
});

afterAll(async () => {
  await cleanupTestUser(userId);
});

function payload(opts: { status: string; productId: string; polarSubscriptionId?: string }) {
  return {
    customer: { externalId: userId },
    data: {
      id: opts.polarSubscriptionId ?? "sub_test_1",
      status: opts.status,
      currentPeriodEnd: "2026-08-06T00:00:00.000Z",
      productId: opts.productId,
    },
  };
}

test("projectSubscriptionEvent: active → upsert; повторен същия payload → 1 ред (идемпотентно)", async () => {
  await BillingDAL.projectSubscriptionEvent(payload({ status: "active", productId: "prod_standard_monthly" }));
  await BillingDAL.projectSubscriptionEvent(payload({ status: "active", productId: "prod_standard_monthly" }));
  const rows = await db.select().from(subscription).where(eq(subscription.userId, userId));
  expect(rows.length).toBe(1);
  expect(rows[0]?.status).toBe("active");
  expect(rows[0]?.plan).toBe("standard");
  expect(rows[0]?.graceUntil).toBeNull();
});

test("projectSubscriptionEvent: active → past_due сетва graceUntil; повторен past_due НЕ го мести (set-once)", async () => {
  await BillingDAL.projectSubscriptionEvent(payload({ status: "past_due", productId: "prod_standard_monthly" }));
  const [first] = await db.select().from(subscription).where(eq(subscription.userId, userId));
  expect(first?.status).toBe("past_due");
  expect(first?.graceUntil).not.toBeNull();
  const firstGrace = first?.graceUntil;

  await BillingDAL.projectSubscriptionEvent(payload({ status: "past_due", productId: "prod_standard_monthly" }));
  const [second] = await db.select().from(subscription).where(eq(subscription.userId, userId));
  expect(second?.graceUntil?.getTime()).toBe(firstGrace?.getTime());

  await BillingDAL.projectSubscriptionEvent(payload({ status: "active", productId: "prod_standard_monthly" }));
  const [cleared] = await db.select().from(subscription).where(eq(subscription.userId, userId));
  expect(cleared?.graceUntil).toBeNull();
});

test("projectSubscriptionEvent: revoked → скрива всички published обяви на потребителя (hiddenBySystem)", async () => {
  const dal = ListingDAL.for(sessionUser);
  const l = await dal.createDraft({ title: "Скрий При Revoke", categoryId, cityId });
  await dal.submit(l.id);

  await BillingDAL.projectSubscriptionEvent(payload({ status: "revoked", productId: "prod_standard_monthly" }));

  const [row] = await db.select().from(listing).where(eq(listing.id, l.id));
  expect(row?.status).toBe("hidden");
  expect(row?.hiddenBySystem).toBe(true);
});

test("projectSubscriptionEvent: downgrade premium→standard с >1 published → скрива всички", async () => {
  await createTestSubscription(userId, { plan: "premium", status: "active" });
  const dal = ListingDAL.for(sessionUser);
  const a = await dal.createDraft({ title: "Downgrade А", categoryId, cityId });
  const b = await dal.createDraft({ title: "Downgrade Б", categoryId, cityId });
  await dal.submit(a.id);
  await dal.submit(b.id);

  await BillingDAL.projectSubscriptionEvent(payload({ status: "active", productId: "prod_standard_monthly" }));

  const [rowA] = await db.select().from(listing).where(eq(listing.id, a.id));
  const [rowB] = await db.select().from(listing).where(eq(listing.id, b.id));
  expect(rowA?.status).toBe("hidden");
  expect(rowA?.hiddenBySystem).toBe(true);
  expect(rowB?.status).toBe("hidden");
  expect(rowB?.hiddenBySystem).toBe(true);
});
```
- [ ] **run (очаква се FAIL — `BillingDAL.projectSubscriptionEvent` не съществува):**
```bash
pnpm vitest run src/data/billing/billing.dal.test.ts
```
- [ ] **имплементация** — добавки в `src/data/billing/billing.dal.ts`. Разшири съществуващия drizzle-orm import ред с `lt`, и schema import реда с `user`: `import { and, count, eq, inArray, lt, ne } from "drizzle-orm";` и `import { listing, setting, subscription, user } from "@/db/schema";`. Типът `Transaction` вече е дефиниран в Задача 2 (`export type Transaction`) — преизползвай го, НЕ декларирай нов алиас:
```ts
// Тесен локален тип: точната форма на Polar webhook payload-а не е потвърдена в код —
// пазим само полетата, които реално ползваме, и парсваме defensively.
export type PolarSubscriptionEventPayload = {
  customer: { externalId: string | null } | null;
  data: {
    id: string;
    status: string;
    currentPeriodEnd: string | Date | null;
    productId: string;
  };
};

function mapStatus(s: string): "active" | "past_due" | "canceled" | "revoked" | null {
  switch (s) {
    case "active": return "active";
    case "past_due": return "past_due";
    case "canceled": return "canceled";
    case "revoked": return "revoked";
    default: return null; // непознат Polar status (напр. trialing) — игнорирай defensively
  }
}

function mapPlan(productId: string): "standard" | "premium" | null {
  if (productId === process.env.POLAR_PRODUCT_STANDARD_MONTHLY || productId === process.env.POLAR_PRODUCT_STANDARD_YEARLY) {
    return "standard";
  }
  if (productId === process.env.POLAR_PRODUCT_PREMIUM_MONTHLY || productId === process.env.POLAR_PRODUCT_PREMIUM_YEARLY) {
    return "premium";
  }
  return null;
}

// Общ helper: скрива ВСИЧКИ published обяви на потребителя (системно, hiddenBySystem=true).
// Ползва се от projectSubscriptionEvent (revoked/downgrade) И от cron expireGracePeriods (Задача 7).
export async function hideAllPublished(tx: Transaction, userId: string): Promise<number> {
  const rows = await tx
    .update(listing)
    .set({ status: "hidden", hiddenBySystem: true, updatedAt: new Date() })
    .where(and(eq(listing.ownerId, userId), eq(listing.status, "published")))
    .returning({ id: listing.id });
  return rows.length;
}

async function countPublished(tx: Transaction, userId: string): Promise<number> {
  const rows = await tx
    .select({ id: listing.id })
    .from(listing)
    .where(and(eq(listing.ownerId, userId), eq(listing.status, "published")));
  return rows.length;
}
```
и добави `projectSubscriptionEvent` като static метод в тялото на съществуващия `class BillingDAL` (до `assertCanPublish` от Задача 2):
```ts
  static async projectSubscriptionEvent(payload: PolarSubscriptionEventPayload): Promise<void> {
    const userId = payload.customer?.externalId;
    if (!userId) {
      console.error("Polar webhook: липсва customer.externalId", payload.data.id);
      return;
    }
    const status = mapStatus(payload.data.status);
    const plan = mapPlan(payload.data.productId);
    if (!status || !plan) {
      console.error("Polar webhook: непознат status/productId", payload.data);
      return;
    }

    const { graceDays } = await getBillingSettings();
    const currentPeriodEnd = payload.data.currentPeriodEnd ? new Date(payload.data.currentPeriodEnd) : null;

    const result = await db.transaction(async (tx) => {
      const [existing] = await tx
        .select({ status: subscription.status, plan: subscription.plan, graceUntil: subscription.graceUntil })
        .from(subscription)
        .where(eq(subscription.userId, userId));

      const graceUntil =
        status === "active"
          ? null
          : status === "past_due"
            ? (existing?.graceUntil ?? new Date(Date.now() + graceDays * 24 * 60 * 60 * 1000))
            : (existing?.graceUntil ?? null);
      const isNewPastDue = status === "past_due" && !existing?.graceUntil;

      await tx
        .insert(subscription)
        .values({ userId, polarSubscriptionId: payload.data.id, plan, status, currentPeriodEnd, graceUntil })
        .onConflictDoUpdate({
          target: subscription.userId,
          set: { polarSubscriptionId: payload.data.id, plan, status, currentPeriodEnd, graceUntil, updatedAt: new Date() },
        });

      let hiddenCount = 0;
      if (status === "revoked") {
        hiddenCount = await hideAllPublished(tx, userId);
      } else if (plan === "standard" && existing?.plan === "premium") {
        const published = await countPublished(tx, userId);
        if (published > 1) hiddenCount = await hideAllPublished(tx, userId);
      }

      return { hiddenCount, isNewPastDue, graceUntil };
    });

    // fire-and-forget email-и СЛЕД транзакцията — Задача 6 замества това с реалното wiring
    // (notifyPastDueEmail(userId, result.graceUntil) / notifyListingsHiddenEmail(userId, result.hiddenCount)).
    void result;
  }
```
- [ ] **имплементация** — `src/lib/auth.ts`: разшири `webhooks({...})` от Задача 4 (единствената промяна в литерала):
```ts
              webhooks({
                secret: process.env.POLAR_WEBHOOK_SECRET!,
                onSubscriptionActive: (payload) => handleSubscriptionEvent(payload),
                onSubscriptionUpdated: (payload) => handleSubscriptionEvent(payload),
                onSubscriptionCanceled: (payload) => handleSubscriptionEvent(payload),
                onSubscriptionRevoked: (payload) => handleSubscriptionEvent(payload),
              }),
```
и добави над `export const auth = ...`:
```ts
import { BillingDAL, type PolarSubscriptionEventPayload } from "@/data/billing/billing.dal";

// Polar е at-least-once; никога не хвърляй тук — webhook route-ът трябва да отговори 200,
// иначе Polar ще retry-ва безкрайно (upsert-ът е идемпотентен, дублирана доставка е ОК).
async function handleSubscriptionEvent(raw: unknown): Promise<void> {
  try {
    await BillingDAL.projectSubscriptionEvent(raw as PolarSubscriptionEventPayload);
  } catch (e) {
    console.error("Polar webhook проекция гръмна", e);
  }
}
```
- [ ] **run pass:**
```bash
pnpm vitest run src/data/billing/billing.dal.test.ts
```
- [ ] **commit:**
```bash
git add src/lib/auth.ts src/data/billing/billing.dal.ts src/data/billing/billing.dal.test.ts
git commit -m "billing: projectSubscriptionEvent — upsert проекция + revoked/downgrade hide"
```

---

### Задача 6: Email-и — `subscriptionPastDueEmail` + `listingsHiddenEmail` + wiring

**Files:**
- Modify: `src/lib/email.ts`
- Modify: `src/lib/email.test.ts`
- Modify: `src/data/billing/billing.dal.ts` (wiring в `projectSubscriptionEvent` от Задача 5)

**Interfaces:**
- Consumes: `sendEmail`, `escapeHtml` (вътрешни в `email.ts`); `getBaseUrl()` от `@/lib/seo`; резултата `{ hiddenCount, isNewPastDue, graceUntil }` от `projectSubscriptionEvent`-ната транзакция (Задача 5).
- Produces: `subscriptionPastDueEmail({ graceUntil: Date })`, `listingsHiddenEmail({ count: number })` (и двете `{subject, html}`); `notifyPastDueEmail(userId, graceUntil)` (вътрешен) и `notifyListingsHiddenEmail(userId, count)` (export от `billing.dal.ts`, ползва се и от Задача 7 cron).

**Решение:** за разлика от `newMessageEmail` (приема `threadUrl` отвън), тук целевият линк е винаги статичен (`/profil/dostavchik/abonament`) — затова `getBaseUrl()` се вика вътре в `email.ts` директно, а не се подава като параметър (запазва точната сигнатура от contract т.15: само `{graceUntil}` / `{count}`). `graceUntil`/`count` не са user input → без `escapeHtml` нужда (само датата се форматира за показ).

- [ ] **failing test** — добавка в `src/lib/email.test.ts`:
```ts
import { subscriptionPastDueEmail, listingsHiddenEmail } from "./email";

test("subscriptionPastDueEmail: съдържа форматираната дата и линк към абонамента", () => {
  const { subject, html } = subscriptionPastDueEmail({ graceUntil: new Date("2026-08-06T00:00:00.000Z") });
  expect(subject).toContain("абонамент");
  expect(html).toContain("/profil/dostavchik/abonament");
  expect(html).toContain("2026");
});

test("listingsHiddenEmail: съдържа броя скрити обяви и линк към абонамента", () => {
  const { subject, html } = listingsHiddenEmail({ count: 3 });
  expect(subject.length).toBeGreaterThan(0);
  expect(html).toContain("3");
  expect(html).toContain("/profil/dostavchik/abonament");
});
```
- [ ] **run (очаква се FAIL — функциите не съществуват):**
```bash
pnpm vitest run src/lib/email.test.ts
```
- [ ] **имплементация** — добавки в `src/lib/email.ts` (в топа добави `import { getBaseUrl } from "@/lib/seo";`, в края на файла):
```ts
export function subscriptionPastDueEmail(input: { graceUntil: Date }): { subject: string; html: string } {
  const subject = "Проблем с плащането на абонамента Ви";
  const dateStr = input.graceUntil.toLocaleDateString("bg-BG", { year: "numeric", month: "long", day: "numeric" });
  const html = `<div style="font-family:sans-serif;line-height:1.5;color:#111">
  <p>Здравейте,</p>
  <p>Опитът за подновяване на абонамента Ви не бе успешен. Имате 7-дневен гратис период да актуализирате начина на плащане.</p>
  <p>Ако не бъде разрешено до <strong>${dateStr}</strong>, публикуваните Ви обяви ще бъдат временно скрити.</p>
  <p><a href="${getBaseUrl()}/profil/dostavchik/abonament">Управление на абонамента</a></p>
</div>`;
  return { subject, html };
}

export function listingsHiddenEmail(input: { count: number }): { subject: string; html: string } {
  const subject = "Обявите Ви бяха скрити";
  const html = `<div style="font-family:sans-serif;line-height:1.5;color:#111">
  <p>Здравейте,</p>
  <p>${input.count} от публикуваните Ви обяви бяха автоматично скрити поради проблем с абонамента.</p>
  <p>Възстановете абонамента, за да ги публикувате отново.</p>
  <p><a href="${getBaseUrl()}/profil/dostavchik/abonament">Управление на абонамента</a></p>
</div>`;
  return { subject, html };
}
```
- [ ] **имплементация** — `src/data/billing/billing.dal.ts`: добави в топа `import { subscriptionPastDueEmail, listingsHiddenEmail, sendEmail } from "@/lib/email";`, после helper-ите и wiring-а:
```ts
async function notifyPastDueEmail(userId: string, graceUntil: Date | null): Promise<void> {
  if (!graceUntil) return;
  const [row] = await db.select({ email: user.email, name: user.name }).from(user).where(eq(user.id, userId));
  if (!row?.email) return;
  const { subject, html } = subscriptionPastDueEmail({ graceUntil });
  await sendEmail({ to: row.email, subject, html });
}

export async function notifyListingsHiddenEmail(userId: string, count: number): Promise<void> {
  if (count <= 0) return;
  const [row] = await db.select({ email: user.email, name: user.name }).from(user).where(eq(user.id, userId));
  if (!row?.email) return;
  const { subject, html } = listingsHiddenEmail({ count });
  await sendEmail({ to: row.email, subject, html });
}
```
и замени placeholder-а `void result;` в края на `projectSubscriptionEvent` (Задача 5) с реалното wiring:
```ts
    // fire-and-forget email-и СЛЕД транзакцията; никога не бутат обработката на webhook-а
    if (result.isNewPastDue) {
      void notifyPastDueEmail(userId, result.graceUntil).catch((e) => console.error("email failed", e));
    }
    if (result.hiddenCount > 0) {
      void notifyListingsHiddenEmail(userId, result.hiddenCount).catch((e) => console.error("email failed", e));
    }
  }
```
- [ ] **run pass:**
```bash
pnpm vitest run src/lib/email.test.ts src/data/billing/billing.dal.test.ts
```
- [ ] **commit:**
```bash
git add src/lib/email.ts src/lib/email.test.ts src/data/billing/billing.dal.ts
git commit -m "billing: subscriptionPastDueEmail/listingsHiddenEmail + fire-and-forget wiring"
```

---

### Задача 7: Cron endpoint `/api/cron/subscriptions` + `expireGracePeriods`

**Files:**
- Modify: `src/data/billing/billing.dal.ts` (добавя `expireGracePeriods` static метод)
- Create: `src/app/api/cron/subscriptions/route.ts`
- Create: `src/app/api/cron/subscriptions/route.test.ts`
- Modify: `src/data/billing/billing.dal.test.ts` (добавка тестове за `expireGracePeriods`)

**Interfaces:**
- Consumes: `hideAllPublished(tx, userId)`, `notifyListingsHiddenEmail(userId, count)` (Задача 5/6, същия файл); `db`, `subscription` schema; `process.env.CRON_SECRET`; `createTestSubscription`/`createTestUser`/`cleanupTestUser` (Задача 1).
- Produces: `BillingDAL.expireGracePeriods(): Promise<{ hidden: number; users: string[] }>` (static); `POST /api/cron/subscriptions` → `{ hidden: number }` | 401.

**Решение:** guard-ът чете `Authorization: Bearer <CRON_SECRET>` идиом (точно както `CRON_SECRET` е дефиниран в `.env.example`); при mismatch/липса → 401 `{error: "UNAUTHORIZED"}`, без тяло-детайли. Обработката е per-user (отделна транзакция за всеки просрочен ред), за да не блокира целия batch при грешка на един потребител; идемпотентно — `hideAllPublished` вече филтрира `WHERE status='published'`, така че повторно изпълнение на cron-а между два реда не удвоява скриването. Email-ът (Задача 6) се праща per-user веднага след скриването му, с реалния брой скрити ЗА ТОЗИ потребител (не агрегат).

- [ ] **failing test** — `src/app/api/cron/subscriptions/route.test.ts` (нов файл):
```ts
import { afterAll, afterEach, beforeAll, expect, test, vi } from "vitest";
import { eq } from "drizzle-orm";
import { db } from "@/db";
import { listing } from "@/db/schema";
import { createTestUser, cleanupTestUser, createTestSubscription, getTestCategoryId, getTestCityId } from "@/test/db-helpers";
import { ListingDAL } from "@/data/catalog/listing.dal";
import type { SessionUser } from "@/data/users/require-user";
import { POST } from "./route";

let userId: string;
let sessionUser: SessionUser;
let categoryId: string;
let cityId: string;

beforeAll(async () => {
  const u = await createTestUser();
  userId = u.id;
  sessionUser = { id: u.id, email: u.email, name: "Тест", isAdmin: false };
  categoryId = await getTestCategoryId();
  cityId = await getTestCityId();
  vi.stubEnv("CRON_SECRET", "test-cron-secret");
});

afterAll(async () => {
  vi.unstubAllEnvs();
  await cleanupTestUser(userId);
});

afterEach(() => {
  vi.unstubAllEnvs();
  vi.stubEnv("CRON_SECRET", "test-cron-secret");
});

function req(auth?: string) {
  return new Request("http://localhost/api/cron/subscriptions", {
    method: "POST",
    headers: auth ? { Authorization: auth } : {},
  });
}

test("401 без валиден CRON_SECRET", async () => {
  const res = await POST(req());
  expect(res.status).toBe(401);
  const res2 = await POST(req("Bearer wrong"));
  expect(res2.status).toBe(401);
});

test("изтекъл гратис → published обявите се скриват (hiddenBySystem) + {hidden: n}", async () => {
  await createTestSubscription(userId, {
    plan: "standard",
    status: "past_due",
    graceUntil: new Date(Date.now() - 24 * 60 * 60 * 1000), // вчера — изтекъл
  });
  const dal = ListingDAL.for(sessionUser);
  const l = await dal.createDraft({ title: "Изтекъл Гратис Тест", categoryId, cityId });
  await dal.submit(l.id);

  const res = await POST(req("Bearer test-cron-secret"));
  expect(res.status).toBe(200);
  const body = await res.json();
  expect(body.hidden).toBeGreaterThanOrEqual(1);

  const [row] = await db.select().from(listing).where(eq(listing.id, l.id));
  expect(row?.status).toBe("hidden");
  expect(row?.hiddenBySystem).toBe(true);
});

test("неизтекъл гратис → no-op (обявата остава published)", async () => {
  const u2 = await createTestUser();
  const u2Session: SessionUser = { id: u2.id, email: u2.email, name: "Тест2", isAdmin: false };
  await createTestSubscription(u2.id, {
    plan: "standard",
    status: "past_due",
    graceUntil: new Date(Date.now() + 24 * 60 * 60 * 1000), // утре — неизтекъл
  });
  const dal = ListingDAL.for(u2Session);
  const l = await dal.createDraft({ title: "Неизтекъл Гратис Тест", categoryId, cityId });
  await dal.submit(l.id);

  await POST(req("Bearer test-cron-secret"));

  const [row] = await db.select().from(listing).where(eq(listing.id, l.id));
  expect(row?.status).toBe("published");

  await cleanupTestUser(u2.id);
});
```
- [ ] **run (очаква се FAIL — `route.ts` не съществува):**
```bash
pnpm vitest run src/app/api/cron/subscriptions/route.test.ts
```
- [ ] **имплементация** — добавка в `src/data/billing/billing.dal.ts` (static метод в класа `BillingDAL`, до `projectSubscriptionEvent`; `lt` вече е добавен към drizzle-orm import реда в Задача 5):
```ts
  static async expireGracePeriods(): Promise<{ hidden: number; users: string[] }> {
    const now = new Date();
    const expired = await db
      .select({ userId: subscription.userId })
      .from(subscription)
      .where(and(eq(subscription.status, "past_due"), lt(subscription.graceUntil, now)));

    let hidden = 0;
    const users: string[] = [];
    for (const { userId } of expired) {
      const count = await db.transaction((tx) => hideAllPublished(tx, userId));
      if (count > 0) {
        hidden += count;
        users.push(userId);
        void notifyListingsHiddenEmail(userId, count).catch((e) => console.error("email failed", e));
      }
    }
    return { hidden, users };
  }
```
- [ ] **имплементация** — `src/app/api/cron/subscriptions/route.ts` (нов файл, идиом от `health/route.ts`):
```ts
import { BillingDAL } from "@/data/billing/billing.dal";

// VPS deploy бележка (ADR 0003 — без Vercel Cron): системен crontab на VPS-а вика този
// endpoint веднъж дневно, напр.:
//   0 3 * * * curl -fsS -X POST -H "Authorization: Bearer $CRON_SECRET" https://<domain>/api/cron/subscriptions
export async function POST(req: Request) {
  const auth = req.headers.get("authorization");
  if (!process.env.CRON_SECRET || auth !== `Bearer ${process.env.CRON_SECRET}`) {
    return Response.json({ error: "UNAUTHORIZED" }, { status: 401 });
  }
  const { hidden } = await BillingDAL.expireGracePeriods();
  return Response.json({ hidden });
}
```
- [ ] **run pass:**
```bash
pnpm vitest run src/app/api/cron/subscriptions/route.test.ts src/data/billing/billing.dal.test.ts
```
- [ ] **commit:**
```bash
git add src/data/billing/billing.dal.ts src/app/api/cron/subscriptions/route.ts src/app/api/cron/subscriptions/route.test.ts src/data/billing/billing.dal.test.ts
git commit -m "billing: cron /api/cron/subscriptions — expireGracePeriods + hide published"
```

---

# Задачи 8–13 (Billing API/DTO/router, i18n, UI, E2E)

> Идиомите по-долу са проверени срещу реалния код (`messaging.dal.ts`, `saved.dal.ts`, `catalog.ts` router, `step-pregled.tsx`, `mobile-bottom-nav.tsx` и др.).

**Резолвнати решения (contract не ги фиксира буквално):**

1. **`SystemHiddenListingDTO.categoryName` е единично поле** (contract), но `getLocale()` от `next-intl/server` НЕ работи надеждно извън `[locale]` route сегмента — `src/app/api/trpc/[trpc]/route.ts` е извън него. Затова `BillingDAL.for(user).mine(locale: "bg" | "en")` приема locale-а изрично като аргумент (threaded от RSC `params` или от tRPC input). Пази DAL-а тестваем в vitest и следва contract буквално (единично поле), само механизмът на locale е избран тук.
2. **`getBillingSettings` е standalone exported функция** (Задача 2), а **`assertCanPublish` е static метод `BillingDAL.assertCanPublish(...)`** (Задача 2) — това са каноничните сигнатури от Задача 2/3. Ако import пътят се различава, assembler-ът/имплементаторът го поправя — сигнатурите остават same.
3. **`dostavchik/layout.tsx` е директно `"use client"`** (не отделен nav файл) — активният pathname state изисква `usePathname()` (client-only hook), точно като `mobile-bottom-nav.tsx`; layout-ът не прави auth проверка (родителският `profil/layout.tsx` вече guard-ва) нито data fetch.
4. **UI компонент тестове:** кодовата база няма `@testing-library`/`*.test.tsx` прецедент — React компонентите се верифицират чрез E2E (Задача 13), не unit тестове (YAGNI/ladder rung 1).

---

### Задача 8: BillingDAL.mine/restoreListings/keepListing + billing.dto.ts + billing tRPC router

**Files:**
- Create: `src/data/billing/billing.dto.ts`
- Modify: `src/data/billing/billing.dal.ts` (добавя `private constructor` + `static for()` + трите инстанс метода към съществуващия `BillingDAL` клас от Задачи 2/3/5/7)
- Create: `src/trpc/routers/billing.ts`
- Modify: `src/trpc/routers/_app.ts` (регистрира `billing` router)
- Test: `src/data/billing/billing-overview.dal.test.ts`

**Interfaces:**
- Consumes: `getBillingSettings(): Promise<{ limits: { standard: number; premiumPerCategory: number }; graceDays: number }>` (Задача 2, **standalone**); `BillingDAL.assertCanPublish(tx, userId: string, categoryId: string, excludeListingId: string): Promise<void>` — **static метод**; хвърля `TRPCError({code:"FORBIDDEN", message:"NO_SUBSCRIPTION"|"LIMIT_REACHED"})` (Задача 2/3); schema `listing.hiddenBySystem` (Задача 1), `subscription`/`setting`/`category`; `createTestSubscription`/`cleanupTestUser` (Задача 1); `SessionUser` (`@/data/users/require-user`).
- Produces: `BillingDAL.for(user).mine(locale)`, `.restoreListings()`, `.keepListing(listingId)`; типове `SubscriptionDTO`, `SystemHiddenListingDTO`, `BillingOverviewDTO`; tRPC `billing.mine`, `billing.restoreListings`, `billing.keepListing` — консумирани от Задачи 10–12.

- [ ] **Стъпка 1 (failing test).** Създай `src/data/billing/billing.dto.ts`:

```ts
export type SubscriptionDTO = {
  plan: "standard" | "premium";
  status: "active" | "past_due" | "canceled" | "revoked";
  currentPeriodEnd: string | null; // ISO
  graceUntil: string | null; // ISO
};

// ponytail: единично categoryName (не bg/en двойка) по contract.md; locale-ът се подава
// изрично в BillingDAL.mine(locale) — виж резолвнато решение #1 в началото на секцията.
export type SystemHiddenListingDTO = {
  id: string;
  title: string;
  categoryName: string;
};

export type BillingOverviewDTO = {
  subscription: SubscriptionDTO | null;
  systemHidden: SystemHiddenListingDTO[];
};
```

Създай `src/data/billing/billing-overview.dal.test.ts`:

```ts
import { afterAll, beforeAll, expect, test } from "vitest";
import { eq } from "drizzle-orm";
import {
  createTestUser, cleanupTestUser, createTestSubscription,
  getTestCategoryId, getTestCityId, testDb,
} from "@/test/db-helpers";
import { listing } from "@/db/schema";
import { ListingDAL } from "@/data/catalog/listing.dal";
import { BillingDAL } from "./billing.dal";
import type { SessionUser } from "@/data/users/require-user";

let vendor: SessionUser, other: SessionUser;
let vendorId: string, otherId: string;
let categoryId: string, categoryNameBg: string;

beforeAll(async () => {
  const v = await createTestUser();
  const o = await createTestUser();
  vendorId = v.id; otherId = o.id;
  vendor = { id: v.id, email: v.email, name: "Вендор", isAdmin: false };
  other = { id: o.id, email: o.email, name: "Друг", isAdmin: false };
  categoryId = await getTestCategoryId();
  categoryNameBg = (await testDb.query.category.findFirst({ where: (c, { eq }) => eq(c.id, categoryId) }))!.nameBg;
});

afterAll(async () => {
  await cleanupTestUser(vendorId);
  await cleanupTestUser(otherId);
});

// помощник: publish + системно скрий (симулира downgrade/grace-expiry hide без да зависи от Задачи 5/7)
async function makeSystemHidden(owner: SessionUser, cityId: string, title: string) {
  const dal = ListingDAL.for(owner);
  const l = await dal.createDraft({ title, categoryId, cityId });
  await dal.submit(l.id);
  await testDb.update(listing).set({ status: "hidden", hiddenBySystem: true }).where(eq(listing.id, l.id));
  return l.id;
}

test("mine(): без ред в subscription → subscription null, systemHidden []", async () => {
  const overview = await BillingDAL.for(other).mine("bg");
  expect(overview.subscription).toBeNull();
  expect(overview.systemHidden).toEqual([]);
});

test("mine(): active subscription + system-hidden обява → categoryName на подадения locale", async () => {
  const cityId = await getTestCityId();
  await createTestSubscription(vendorId, { plan: "premium", status: "active" });
  const hiddenId = await makeSystemHidden(vendor, cityId, "Систем Скрита");
  const overview = await BillingDAL.for(vendor).mine("bg");
  expect(overview.subscription).toMatchObject({ plan: "premium", status: "active" });
  expect(overview.systemHidden).toContainEqual({ id: hiddenId, title: "Систем Скрита", categoryName: categoryNameBg });
});

test("keepListing(): чужда обява → NOT_FOUND (не FORBIDDEN, без enumeration)", async () => {
  const cityId = await getTestCityId();
  const foreignId = await makeSystemHidden(other, cityId, "Чужда Скрита");
  await expect(BillingDAL.for(vendor).keepListing(foreignId)).rejects.toMatchObject({ code: "NOT_FOUND" });
});

test("keepListing(): публикува избраната, скрива останалите published сестри на собственика", async () => {
  const cityId = await getTestCityId();
  const dal = ListingDAL.for(vendor);
  const a = await dal.createDraft({ title: "Запази А", categoryId, cityId });
  await dal.submit(a.id);
  const bId = await makeSystemHidden(vendor, cityId, "Запази Б");
  await BillingDAL.for(vendor).keepListing(bId);
  const [rowA] = await testDb.select().from(listing).where(eq(listing.id, a.id));
  const [rowB] = await testDb.select().from(listing).where(eq(listing.id, bId));
  expect(rowA?.status).toBe("hidden");
  expect(rowA?.hiddenBySystem).toBe(true);
  expect(rowB?.status).toBe("published");
  expect(rowB?.hiddenBySystem).toBe(false);
});

test("restoreListings(): без subscription → NO_SUBSCRIPTION", async () => {
  await expect(BillingDAL.for(other).restoreListings()).rejects.toMatchObject({
    code: "FORBIDDEN", message: "NO_SUBSCRIPTION",
  });
});

test("restoreListings(): standard с 2 hidden обяви (лимит=1) → LIMIT_REACHED, нищо не се променя", async () => {
  const u = await createTestUser();
  const uu: SessionUser = { id: u.id, email: u.email, name: "Стандарт", isAdmin: false };
  await createTestSubscription(u.id, { plan: "standard", status: "active" });
  const cityId = await getTestCityId();
  const h1 = await makeSystemHidden(uu, cityId, "Ст 1");
  const h2 = await makeSystemHidden(uu, cityId, "Ст 2");
  await expect(BillingDAL.for(uu).restoreListings()).rejects.toMatchObject({
    code: "FORBIDDEN", message: "LIMIT_REACHED",
  });
  const [row1] = await testDb.select().from(listing).where(eq(listing.id, h1));
  const [row2] = await testDb.select().from(listing).where(eq(listing.id, h2));
  expect(row1?.status).toBe("hidden");
  expect(row2?.status).toBe("hidden");
  await cleanupTestUser(u.id);
});

test("restoreListings(): premium, 2 hidden в 1 категория (лимит=2/категория) → и двете се възстановяват", async () => {
  const u = await createTestUser();
  const uu: SessionUser = { id: u.id, email: u.email, name: "Премиум", isAdmin: false };
  await createTestSubscription(u.id, { plan: "premium", status: "active" });
  const cityId = await getTestCityId();
  const h1 = await makeSystemHidden(uu, cityId, "Пр 1");
  const h2 = await makeSystemHidden(uu, cityId, "Пр 2");
  const result = await BillingDAL.for(uu).restoreListings();
  expect(result.restored).toBe(2);
  const [row1] = await testDb.select().from(listing).where(eq(listing.id, h1));
  const [row2] = await testDb.select().from(listing).where(eq(listing.id, h2));
  expect(row1?.status).toBe("published");
  expect(row2?.status).toBe("published");
  await cleanupTestUser(u.id);
});
```

- [ ] **Стъпка 2 (run — очаквано FAIL):** `pnpm test billing-overview.dal.test.ts` — чупи се, `BillingDAL.for(...).mine/restoreListings/keepListing` не съществуват.

- [ ] **Стъпка 3 (имплементация — ПЪЛЕН код).** В `src/data/billing/billing.dal.ts` (файлът вече съществува с `BillingDAL` класа + `assertCanPublish`/`getBillingSettings`/`projectSubscriptionEvent`/`expireGracePeriods` от Задачи 2/3/5/7). Разшири импортите ако липсват — увери се, че drizzle-orm има `count`, `inArray`, `ne`; добави `category` към schema import; добави DTO и `SessionUser` import-и:

```ts
// увери се, че тези символи присъстват в billing.dal.ts (Задача 2 вече внесе повечето):
import { and, count, eq, inArray, lt, ne } from "drizzle-orm";
import { TRPCError } from "@trpc/server";
import { category, listing, setting, subscription, user } from "@/db/schema";
import type { SessionUser } from "@/data/users/require-user";
import type { BillingOverviewDTO, SubscriptionDTO, SystemHiddenListingDTO } from "./billing.dto";
// getBillingSettings/assertCanPublish са в същия файл (Задача 2) — без нов import
```

Класът `BillingDAL` дотук има само static методи (Задачи 2/5/7). Добави `private constructor` + `static for()` (инстанс методите ползват `this.user`) и трите инстанс метода в тялото на класа:

```ts
  // конструктор + фабрика — mine/restoreListings/keepListing ползват this.user.
  // (static assertCanPublish/projectSubscriptionEvent/expireGracePeriods остават непроменени)
  private constructor(private readonly user: SessionUser) {}
  static for(user: SessionUser): BillingDAL {
    return new BillingDAL(user);
  }

  async mine(locale: "bg" | "en"): Promise<BillingOverviewDTO> {
    const [subRow] = await db.select().from(subscription).where(eq(subscription.userId, this.user.id));
    const hiddenRows = await db
      .select({
        id: listing.id,
        title: listing.title,
        categoryNameBg: category.nameBg,
        categoryNameEn: category.nameEn,
      })
      .from(listing)
      .innerJoin(category, eq(listing.categoryId, category.id))
      .where(and(
        eq(listing.ownerId, this.user.id),
        eq(listing.status, "hidden"),
        eq(listing.hiddenBySystem, true),
      ));
    const sub: SubscriptionDTO | null = subRow ? {
      plan: subRow.plan,
      status: subRow.status,
      currentPeriodEnd: subRow.currentPeriodEnd?.toISOString() ?? null,
      graceUntil: subRow.graceUntil?.toISOString() ?? null,
    } : null;
    const systemHidden: SystemHiddenListingDTO[] = hiddenRows.map((r) => ({
      id: r.id,
      title: r.title,
      categoryName: locale === "bg" ? r.categoryNameBg : r.categoryNameEn,
    }));
    return { subscription: sub, systemHidden };
  }

  async restoreListings(): Promise<{ restored: number }> {
    const userId = this.user.id;
    return db.transaction(async (tx) => {
      const [subRow] = await tx.select().from(subscription).where(eq(subscription.userId, userId));
      const activeForPublishing = subRow && (
        subRow.status === "active" ||
        (subRow.status === "past_due" && subRow.graceUntil !== null && subRow.graceUntil > new Date())
      );
      if (!activeForPublishing) throw new TRPCError({ code: "FORBIDDEN", message: "NO_SUBSCRIPTION" });

      const hiddenRows = await tx
        .select({ id: listing.id, categoryId: listing.categoryId })
        .from(listing)
        .where(and(eq(listing.ownerId, userId), eq(listing.status, "hidden"), eq(listing.hiddenBySystem, true)));
      if (hiddenRows.length === 0) return { restored: 0 };

      const publishedByCategory = await tx
        .select({ categoryId: listing.categoryId, n: count() })
        .from(listing)
        .where(and(eq(listing.ownerId, userId), eq(listing.status, "published")))
        .groupBy(listing.categoryId);

      const { limits: { standard, premiumPerCategory } } = await getBillingSettings();
      if (subRow!.plan === "standard") {
        const publishedTotal = publishedByCategory.reduce((sum, r) => sum + r.n, 0);
        if (publishedTotal + hiddenRows.length > standard) {
          throw new TRPCError({ code: "FORBIDDEN", message: "LIMIT_REACHED" });
        }
      } else {
        const publishedMap = new Map(publishedByCategory.map((r) => [r.categoryId, r.n]));
        const hiddenByCategory = new Map<string, number>();
        for (const r of hiddenRows) hiddenByCategory.set(r.categoryId, (hiddenByCategory.get(r.categoryId) ?? 0) + 1);
        for (const [catId, hiddenCount] of hiddenByCategory) {
          const already = publishedMap.get(catId) ?? 0;
          if (already + hiddenCount > premiumPerCategory) {
            throw new TRPCError({ code: "FORBIDDEN", message: "LIMIT_REACHED" });
          }
        }
      }

      const ids = hiddenRows.map((r) => r.id);
      await tx.update(listing)
        .set({ status: "published", hiddenBySystem: false, publishedAt: new Date(), updatedAt: new Date() })
        .where(and(
          eq(listing.ownerId, userId),
          inArray(listing.id, ids),
          eq(listing.status, "hidden"),
          eq(listing.hiddenBySystem, true),
        ));
      return { restored: ids.length };
    });
  }

  async keepListing(listingId: string): Promise<void> {
    const userId = this.user.id;
    await db.transaction(async (tx) => {
      const [row] = await tx.select().from(listing).where(eq(listing.id, listingId));
      // чужда обява ИЛИ не е system-hidden → NOT_FOUND, никога FORBIDDEN (без enumeration, contract т.7)
      if (!row || row.ownerId !== userId || row.status !== "hidden" || !row.hiddenBySystem) {
        throw new TRPCError({ code: "NOT_FOUND" });
      }
      await BillingDAL.assertCanPublish(tx, userId, row.categoryId, listingId);
      await tx.update(listing)
        .set({ status: "published", hiddenBySystem: false, publishedAt: new Date(), updatedAt: new Date() })
        .where(and(eq(listing.id, listingId), eq(listing.status, "hidden")));
      // скрий всички останали published сестри на собственика (запазваме само избраната)
      await tx.update(listing)
        .set({ status: "hidden", hiddenBySystem: true, updatedAt: new Date() })
        .where(and(eq(listing.ownerId, userId), ne(listing.id, listingId), eq(listing.status, "published")));
    });
  }
```

- [ ] **Стъпка 4 (run pass):** `pnpm test billing-overview.dal.test.ts` — зелено.

- [ ] **Стъпка 5 (tRPC router).** Създай `src/trpc/routers/billing.ts`:

```ts
import { z } from "zod";
import { revalidateTag } from "next/cache";
import { createTRPCRouter, protectedProcedure } from "../init";
import { BillingDAL } from "@/data/billing/billing.dal";

export const billingRouter = createTRPCRouter({
  mine: protectedProcedure
    .input(z.object({ locale: z.enum(["bg", "en"]) }))
    .query(({ ctx, input }) => BillingDAL.for(ctx.user).mine(input.locale)),
  restoreListings: protectedProcedure.mutation(async ({ ctx }) => {
    const r = await BillingDAL.for(ctx.user).restoreListings();
    revalidateTag("listings", { expire: 0 });
    return r;
  }),
  keepListing: protectedProcedure
    .input(z.object({ listingId: z.uuid() }))
    .mutation(async ({ ctx, input }) => {
      await BillingDAL.for(ctx.user).keepListing(input.listingId);
      revalidateTag("listings", { expire: 0 });
    }),
});
```

В `src/trpc/routers/_app.ts` добави import + регистрация:

```ts
import { billingRouter } from "./billing";
// ...
export const appRouter = createTRPCRouter({
  // ...existing
  billing: billingRouter,
});
```

- [ ] **Стъпка 6 (commit):**

```bash
git add src/data/billing/billing.dto.ts src/data/billing/billing.dal.ts src/data/billing/billing-overview.dal.test.ts src/trpc/routers/billing.ts src/trpc/routers/_app.ts
git commit -m "feat: billing.mine/restoreListings/keepListing DAL + tRPC router"
```

---

### Задача 9: i18n `Billing` namespace (bg + en)

**Files:** Modify `messages/bg.json`, `messages/en.json` (нов top-level ключ `Billing` след `Messages`, byte-mirrored позиции).

**Interfaces:** Produces ключовете, консумирани буквално от Задачи 10–12 (`Billing.nav.*`, `Billing.plans.*`, `Billing.status.*`, `Billing.picker.*`, `Billing.gate.*`, плюс top-level `Billing.*`). Пълно покритие проверено срещу Задачи 10/11/12 — всеки `t(...)` ключ там присъства тук.

- [ ] **Стъпка 1:** Добави в `messages/bg.json`, веднага след затварящата `}` на `"Messages"`:

```json
  "Billing": {
    "pageTitle": "Абонамент",
    "nav": {
      "listings": "Обяви",
      "subscription": "Абонамент"
    },
    "plans": {
      "standard": {
        "name": "Standard",
        "feature1": "1 публикувана обява в 1 категория",
        "feature2": "Управление на календар и резервации",
        "feature3": "Промотиране — докупва се еднократно"
      },
      "premium": {
        "name": "Premium",
        "feature1": "До 2 публикувани обяви във всяка категория",
        "feature2": "Управление на календар и резервации",
        "feature3": "Промотиране включено в плана"
      },
      "monthly": "На месец",
      "yearly": "На година",
      "priceHint": "Цената се показва при плащане",
      "checkoutCta": "Абонирай се"
    },
    "status": {
      "active": "Активен",
      "past_due": "Просрочено плащане",
      "canceled": "Отказан",
      "revoked": "Прекратен"
    },
    "noSubscriptionTitle": "Нямаш активен абонамент",
    "noSubscriptionHint": "Абонирай се, за да публикуваш обяви.",
    "pastDueWarning": "Плащането не мина. Ако не се поднови до {date}, обявите ти ще бъдат скрити.",
    "portalCta": "Управление на плащанията",
    "restoreCta": "Възстанови обявите",
    "restoring": "Възстановяване…",
    "restoreErrorLimit": "Лимитът на плана е достигнат — избери коя обява да остане публикувана.",
    "restoreErrorGeneric": "Грешка при възстановяването. Опитай пак.",
    "systemHiddenCount": "{count, plural, one {# скрита системно обява} other {# скрити системно обяви}}",
    "picker": {
      "title": "Избери коя обява да остане публикувана",
      "hint": "Планът позволява по-малко публикувани обяви. Останалите остават скрити, но пазят ревютата и историята си.",
      "confirm": "Публикувай избраната",
      "confirming": "Публикуване…",
      "error": "Грешка при публикуването. Опитай пак."
    },
    "gate": {
      "noSubscription": "Трябва активен абонамент, за да публикуваш обява.",
      "limitReached": "Достигнат е лимитът на плана ти за тази категория.",
      "cta": "Виж абонамента"
    }
  }
```

Огледален английски вариант в `messages/en.json`, СЪЩАТА позиция (след `"Messages"`), СЪЩИТЕ ключове:

```json
  "Billing": {
    "pageTitle": "Subscription",
    "nav": {
      "listings": "Listings",
      "subscription": "Subscription"
    },
    "plans": {
      "standard": {
        "name": "Standard",
        "feature1": "1 published listing in 1 category",
        "feature2": "Calendar and booking management",
        "feature3": "Promotion — one-time purchase"
      },
      "premium": {
        "name": "Premium",
        "feature1": "Up to 2 published listings in every category",
        "feature2": "Calendar and booking management",
        "feature3": "Promotion included in the plan"
      },
      "monthly": "Monthly",
      "yearly": "Yearly",
      "priceHint": "Price shown at checkout",
      "checkoutCta": "Subscribe"
    },
    "status": {
      "active": "Active",
      "past_due": "Payment past due",
      "canceled": "Canceled",
      "revoked": "Revoked"
    },
    "noSubscriptionTitle": "No active subscription",
    "noSubscriptionHint": "Subscribe to publish listings.",
    "pastDueWarning": "Payment failed. If not renewed by {date}, your listings will be hidden.",
    "portalCta": "Manage billing",
    "restoreCta": "Restore listings",
    "restoring": "Restoring…",
    "restoreErrorLimit": "Plan limit reached — pick which listing stays published.",
    "restoreErrorGeneric": "Error restoring listings. Try again.",
    "systemHiddenCount": "{count, plural, one {# listing hidden by the system} other {# listings hidden by the system}}",
    "picker": {
      "title": "Pick which listing stays published",
      "hint": "Your plan allows fewer published listings. The rest stay hidden but keep their reviews and history.",
      "confirm": "Publish selected",
      "confirming": "Publishing…",
      "error": "Error publishing. Try again."
    },
    "gate": {
      "noSubscription": "You need an active subscription to publish a listing.",
      "limitReached": "You've reached your plan's limit for this category.",
      "cta": "View subscription"
    }
  }
```

- [ ] **Стъпка 2 (run pass):** `pnpm exec node -e "JSON.parse(require('fs').readFileSync('messages/bg.json','utf8')); JSON.parse(require('fs').readFileSync('messages/en.json','utf8')); console.log('valid json')"` — валидира синтаксиса; допълнително `pnpm build` (Задача 10+) ще падне при липсващ ключ.

- [ ] **Стъпка 3 (commit):**

```bash
git add messages/bg.json messages/en.json
git commit -m "feat: add Billing i18n namespace (bg+en)"
```

---

### Задача 10: dostavchik/layout.tsx nav + abonament/page.tsx + SubscriptionStatusCard

**Files:**
- Create: `src/app/[locale]/profil/dostavchik/layout.tsx`
- Create: `src/app/[locale]/profil/dostavchik/abonament/page.tsx`
- Create: `src/components/billing/subscription-status-card.tsx`
- Test: няма unit test (виж резолвнато решение #4) — верификация в Задача 13.

**Interfaces:**
- Consumes: `BillingDAL.for(user).mine(locale)` (Задача 8, RSC direct read, ADR 0002); `requireUser()` (`@/data/users/require-user`); `authClient.customer.portal()` (Задача 4 — Polar plugin в `auth-client.ts`); `useTRPC().billing.restoreListings` (Задача 8); `Billing.*` i18n ключове (Задача 9); `DowngradeListingPicker` (Задача 11, import директно тук).
- Produces: `/profil/dostavchik/abonament` route; `<SubscriptionStatusCard subscription systemHidden />` (използван от `abonament/page.tsx`).

- [ ] **Стъпка 1 (без failing test — RSC route + client presentational компонент, верифицирано визуално в Задача 13).**

`src/app/[locale]/profil/dostavchik/layout.tsx`:

```tsx
"use client";
import { useTranslations } from "next-intl";
import { Link, usePathname } from "@/i18n/navigation";
import { cn } from "@/lib/utils";

// ponytail: "use client" за целия layout — активният pathname state изисква usePathname()
// (client-only hook, точно като mobile-bottom-nav.tsx); auth guard-ва родителският profil/layout.tsx.
export default function DostavchikLayout({ children }: { children: React.ReactNode }) {
  const t = useTranslations("Billing.nav");
  const pathname = usePathname();
  const tabs = [
    { href: "/profil/dostavchik/obiavi", label: t("listings") },
    { href: "/profil/dostavchik/abonament", label: t("subscription") },
  ] as const;

  return (
    <div className="space-y-6">
      <nav className="flex gap-1 border-b border-border">
        {tabs.map(({ href, label }) => {
          const active = pathname === href || pathname.startsWith(`${href}/`);
          return (
            <Link
              key={href}
              href={href}
              aria-current={active ? "page" : undefined}
              className={cn(
                "flex min-h-11 items-center border-b-2 px-3 text-sm font-medium transition-colors",
                active ? "border-primary text-primary" : "border-transparent text-muted-foreground hover:text-foreground",
              )}
            >
              {label}
            </Link>
          );
        })}
      </nav>
      {children}
    </div>
  );
}
```

`src/app/[locale]/profil/dostavchik/abonament/page.tsx`:

```tsx
import { setRequestLocale, getTranslations } from "next-intl/server";
import { requireUser } from "@/data/users/require-user";
import { BillingDAL } from "@/data/billing/billing.dal";
import { SubscriptionStatusCard } from "@/components/billing/subscription-status-card";
import { PlanPricingTable } from "@/components/billing/plan-pricing-table";

export default async function AbonamentPage({
  params,
}: {
  params: Promise<{ locale: "bg" | "en" }>;
}) {
  const { locale } = await params;
  setRequestLocale(locale);
  const t = await getTranslations("Billing");
  const user = await requireUser();
  const overview = await BillingDAL.for(user).mine(locale);

  return (
    <main className="space-y-6">
      <h1 className="font-serif text-3xl">{t("pageTitle")}</h1>
      <SubscriptionStatusCard subscription={overview.subscription} systemHidden={overview.systemHidden} />
      <PlanPricingTable />
    </main>
  );
}
```

`src/components/billing/subscription-status-card.tsx`:

```tsx
"use client";
import { useState } from "react";
import { useMutation } from "@tanstack/react-query";
import { useLocale, useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
import { authClient } from "@/lib/auth-client";
import { useTRPC } from "@/trpc/client";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { BillingOverviewDTO } from "@/data/billing/billing.dto";
import { DowngradeListingPicker } from "./downgrade-listing-picker";

type RestoreErrorKey = "generic" | "limitReached";

export function SubscriptionStatusCard({
  subscription,
  systemHidden,
}: {
  subscription: BillingOverviewDTO["subscription"];
  systemHidden: BillingOverviewDTO["systemHidden"];
}) {
  const t = useTranslations("Billing");
  const locale = useLocale();
  const trpc = useTRPC();
  const router = useRouter();
  const [restoreError, setRestoreError] = useState<RestoreErrorKey | null>(null);
  // standard + >1 system-hidden → picker-ът е задължителен веднага (contract р.11); premium
  // over-limit случаят се разкрива само след LIMIT_REACHED от restore (виж onError по-долу).
  const [showPicker, setShowPicker] = useState(
    subscription?.plan === "standard" && systemHidden.length > 1,
  );

  const restore = useMutation(
    trpc.billing.restoreListings.mutationOptions({
      onSuccess: () => { setRestoreError(null); router.refresh(); },
      onError: (err) => {
        if (err.data?.code === "FORBIDDEN" && err.message === "LIMIT_REACHED") {
          setRestoreError("limitReached");
          setShowPicker(true);
        } else {
          setRestoreError("generic");
        }
      },
    }),
  );

  if (!subscription) {
    return (
      <Card>
        <CardContent className="space-y-2 p-6">
          <p className="font-medium">{t("noSubscriptionTitle")}</p>
          <p className="text-sm text-muted-foreground">{t("noSubscriptionHint")}</p>
        </CardContent>
      </Card>
    );
  }

  const graceDate = subscription.graceUntil
    ? new Intl.DateTimeFormat(locale, { dateStyle: "long" }).format(new Date(subscription.graceUntil))
    : null;

  return (
    <Card>
      <CardContent className="space-y-4 p-6">
        <div className="flex items-center justify-between gap-3">
          <div>
            <p className="font-serif text-xl">{t(`plans.${subscription.plan}.name`)}</p>
            <p className="text-sm text-muted-foreground">{t(`status.${subscription.status}`)}</p>
          </div>
          <Button variant="outline" onClick={() => { void authClient.customer.portal(); }}>
            {t("portalCta")}
          </Button>
        </div>
        {subscription.status === "past_due" && graceDate && (
          <p role="alert" className="rounded-md border border-destructive/40 bg-destructive/10 p-3 text-sm">
            {t("pastDueWarning", { date: graceDate })}
          </p>
        )}
        {systemHidden.length > 0 && (
          <div className="space-y-2 border-t border-border pt-4">
            <p className="text-sm text-muted-foreground">
              {t("systemHiddenCount", { count: systemHidden.length })}
            </p>
            <Button disabled={restore.isPending} onClick={() => { setRestoreError(null); restore.mutate(); }}>
              {restore.isPending ? t("restoring") : t("restoreCta")}
            </Button>
            {restoreError && (
              <p role="alert" className="text-sm text-destructive">
                {t(restoreError === "limitReached" ? "restoreErrorLimit" : "restoreErrorGeneric")}
              </p>
            )}
          </div>
        )}
        {showPicker && <DowngradeListingPicker listings={systemHidden} />}
      </CardContent>
    </Card>
  );
}
```

- [ ] **Стъпка 2 (типове/build):** `pnpm build` (или `pnpm lint`) — трябва да мине без TS грешки. `DowngradeListingPicker` идва от Задача 11 (import разчита на нейния файл да съществува — ако Задачи 10/11 се изпълняват последователно от същия assembler, Задача 11 или трябва да предхожда build проверката тук, или `pnpm build` на тази стъпка се отлага до след Задача 11).

- [ ] **Стъпка 3 (commit):**

```bash
git add src/app/\[locale\]/profil/dostavchik/layout.tsx src/app/\[locale\]/profil/dostavchik/abonament/page.tsx src/components/billing/subscription-status-card.tsx
git commit -m "feat: dostavchik nav + abonament page + SubscriptionStatusCard"
```

---

### Задача 11: PlanPricingTable + DowngradeListingPicker

**Files:**
- Create: `src/components/billing/plan-pricing-table.tsx`
- Create: `src/components/billing/downgrade-listing-picker.tsx`

**Interfaces:**
- Consumes: `authClient.checkout({ slug })` (Задача 4 Polar plugin); `authClient.useSession()` (съществуващ idiom, `save-button.tsx`); `useTRPC().billing.keepListing` (Задача 8); `SystemHiddenListingDTO` (Задача 8); `Billing.plans.*`/`Billing.picker.*` i18n (Задача 9).
- Produces: `<PlanPricingTable />` (използван от `abonament/page.tsx`, Задача 10); `<DowngradeListingPicker listings={...} />` (използван от `SubscriptionStatusCard`, Задача 10).

- [ ] **Стъпка 1 (без failing test — виж резолвнато решение #4).**

`src/components/billing/plan-pricing-table.tsx`:

```tsx
"use client";
import { useState } from "react";
import { useTranslations } from "next-intl";
import { Link } from "@/i18n/navigation";
import { authClient } from "@/lib/auth-client";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { cn } from "@/lib/utils";

type Cycle = "monthly" | "yearly";
type PlanKey = "standard" | "premium";

// Polar checkout slugs — фиксирани в Задача 4 (checkout({ products: [...] })), НЕ цени в кода.
const CHECKOUT_SLUGS: Record<PlanKey, Record<Cycle, string>> = {
  standard: { monthly: "standard-monthly", yearly: "standard-yearly" },
  premium: { monthly: "premium-monthly", yearly: "premium-yearly" },
};

export function PlanPricingTable() {
  const t = useTranslations("Billing.plans");
  const { data: session } = authClient.useSession();
  const [cycle, setCycle] = useState<Cycle>("monthly");

  return (
    <div className="space-y-4">
      <div className="inline-flex rounded-md border border-border p-1">
        {(["monthly", "yearly"] as const).map((c) => (
          <button
            key={c}
            type="button"
            aria-pressed={cycle === c}
            onClick={() => setCycle(c)}
            className={cn(
              "min-h-11 rounded-sm px-3 text-sm transition-colors",
              cycle === c ? "bg-primary text-primary-foreground" : "text-muted-foreground hover:text-foreground",
            )}
          >
            {t(c)}
          </button>
        ))}
      </div>
      <div className="grid gap-4 sm:grid-cols-2">
        {(["standard", "premium"] as const).map((plan) => (
          <Card key={plan}>
            <CardHeader>
              <CardTitle>{t(`${plan}.name`)}</CardTitle>
            </CardHeader>
            <CardContent className="space-y-3">
              <ul className="space-y-1 text-sm">
                <li>{t(`${plan}.feature1`)}</li>
                <li>{t(`${plan}.feature2`)}</li>
                <li>{t(`${plan}.feature3`)}</li>
              </ul>
              <p className="text-xs text-muted-foreground">{t("priceHint")}</p>
              {session ? (
                <Button onClick={() => { void authClient.checkout({ slug: CHECKOUT_SLUGS[plan][cycle] }); }}>
                  {t("checkoutCta")}
                </Button>
              ) : (
                <Button asChild>
                  <Link href="/vhod">{t("checkoutCta")}</Link>
                </Button>
              )}
            </CardContent>
          </Card>
        ))}
      </div>
    </div>
  );
}
```

`src/components/billing/downgrade-listing-picker.tsx`:

```tsx
"use client";
import { useState } from "react";
import { useMutation } from "@tanstack/react-query";
import { useTranslations } from "next-intl";
import { useRouter } from "@/i18n/navigation";
import { useTRPC } from "@/trpc/client";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import type { SystemHiddenListingDTO } from "@/data/billing/billing.dto";

export function DowngradeListingPicker({ listings }: { listings: SystemHiddenListingDTO[] }) {
  const t = useTranslations("Billing.picker");
  const trpc = useTRPC();
  const router = useRouter();
  const [selected, setSelected] = useState<string | null>(null);
  const [error, setError] = useState(false);

  const keep = useMutation(
    trpc.billing.keepListing.mutationOptions({
      onSuccess: () => router.refresh(),
      onError: () => setError(true),
    }),
  );

  if (listings.length === 0) return null;

  return (
    <Card>
      <CardContent className="space-y-3 p-6">
        <p className="font-medium">{t("title")}</p>
        <p className="text-sm text-muted-foreground">{t("hint")}</p>
        <fieldset className="space-y-1">
          {listings.map((l) => (
            <label
              key={l.id}
              className="flex min-h-11 items-center gap-3 rounded-md border border-border p-2 text-sm has-checked:border-primary"
            >
              <input
                type="radio"
                name="keep-listing"
                value={l.id}
                checked={selected === l.id}
                onChange={() => setSelected(l.id)}
                className="size-4"
              />
              <span>{l.title}</span>
              <span className="text-muted-foreground">· {l.categoryName}</span>
            </label>
          ))}
        </fieldset>
        {error && <p role="alert" className="text-sm text-destructive">{t("error")}</p>}
        <Button
          disabled={!selected || keep.isPending}
          onClick={() => {
            if (!selected) return;
            setError(false);
            keep.mutate({ listingId: selected });
          }}
        >
          {keep.isPending ? t("confirming") : t("confirm")}
        </Button>
      </CardContent>
    </Card>
  );
}
```

- [ ] **Стъпка 2 (build):** `pnpm build` — сега вкл. Задача 10's import на `DowngradeListingPicker` трябва да резолва.

- [ ] **Стъпка 3 (commit):**

```bash
git add src/components/billing/plan-pricing-table.tsx src/components/billing/downgrade-listing-picker.tsx
git commit -m "feat: PlanPricingTable + DowngradeListingPicker"
```

---

### Задача 12: Publish-gate UI — step-pregled.tsx + listing-actions.tsx

**Files:** Modify `src/components/vendor/wizard/step-pregled.tsx`, `src/components/vendor/listing-actions.tsx`.

**Interfaces:**
- Consumes: `trpc.catalog.listing.submit`/`.unhide` мутациите вече хвърлят `TRPCError({code:"FORBIDDEN", message:"NO_SUBSCRIPTION"|"LIMIT_REACHED"})` (Задача 3 wiring в `listing.dal.ts`/`catalog.ts` router); `Billing.gate.*` i18n (Задача 9).
- Produces: няма нов публичен API — UI поведение само.

- [ ] **Стъпка 1 (без failing unit test — виж резолвнато решение #4; поведението се проверява в Задача 13, стъпка "publish gate").**

В `step-pregled.tsx`, замени:

```tsx
import { useRouter } from "@/i18n/navigation";
```
с:
```tsx
import { Link, useRouter } from "@/i18n/navigation";
```

Замени:
```tsx
  const [error, setError] = useState(false);
```
с:
```tsx
  const tb = useTranslations("Billing");
  const [errorKey, setErrorKey] = useState<"errorSave" | "noSubscription" | "limitReached" | null>(null);
```

Замени submit мутацията:
```tsx
  const submit = useMutation(
    trpc.catalog.listing.submit.mutationOptions({
      onSuccess: () => { toast.success(t("published")); router.refresh(); },
      onError: () => setError(true),
    }),
  );
```
с:
```tsx
  const submit = useMutation(
    trpc.catalog.listing.submit.mutationOptions({
      onSuccess: () => { toast.success(t("published")); router.refresh(); },
      // billing entitlement грешки (err.data?.code==="FORBIDDEN" + err.message) → отделен CTA към абонамента;
      // idiom по inquiry-form.tsx (err.data?.code branch)
      onError: (err) => {
        if (err.data?.code === "FORBIDDEN" && err.message === "NO_SUBSCRIPTION") setErrorKey("noSubscription");
        else if (err.data?.code === "FORBIDDEN" && err.message === "LIMIT_REACHED") setErrorKey("limitReached");
        else setErrorKey("errorSave");
      },
    }),
  );
```

Замени hide мутацията's `onError: () => setError(true)` с `onError: () => setErrorKey("errorSave")`.

Замени error render блока:
```tsx
      {error && <p role="alert" className="text-sm text-destructive">{tv("errorSave")}</p>}
```
с:
```tsx
      {errorKey && (
        <p role="alert" className="text-sm text-destructive">
          {errorKey === "errorSave" ? tv("errorSave") : tb(errorKey === "noSubscription" ? "gate.noSubscription" : "gate.limitReached")}
          {errorKey !== "errorSave" && (
            <>
              {" "}
              <Link href="/profil/dostavchik/abonament" className="underline">{tb("gate.cta")}</Link>
            </>
          )}
        </p>
      )}
```

И двата `onClick` handler-а (`submit.mutate`/`hide.mutate`) — замени `setError(false)` с `setErrorKey(null)`.

В `listing-actions.tsx`, замени:
```tsx
import { useRouter } from "@/i18n/navigation";
```
с:
```tsx
import { Link, useRouter } from "@/i18n/navigation";
```

Замени:
```tsx
  const [error, setError] = useState(false);
  const hide = useMutation(trpc.catalog.listing.hide.mutationOptions({ onSuccess: refresh, onError: () => setError(true) }));
  const unhide = useMutation(trpc.catalog.listing.unhide.mutationOptions({ onSuccess: refresh, onError: () => setError(true) }));
```
с:
```tsx
  const tb = useTranslations("Billing");
  const [errorKey, setErrorKey] = useState<"errorSave" | "noSubscription" | "limitReached" | null>(null);
  const hide = useMutation(trpc.catalog.listing.hide.mutationOptions({ onSuccess: refresh, onError: () => setErrorKey("errorSave") }));
  const unhide = useMutation(trpc.catalog.listing.unhide.mutationOptions({
    onSuccess: refresh,
    onError: (err) => {
      if (err.data?.code === "FORBIDDEN" && err.message === "NO_SUBSCRIPTION") setErrorKey("noSubscription");
      else if (err.data?.code === "FORBIDDEN" && err.message === "LIMIT_REACHED") setErrorKey("limitReached");
      else setErrorKey("errorSave");
    },
  }));
```

В `status === "published"` клона: `setError(false)` → `setErrorKey(null)`, `{error && <p role="alert" className="text-sm text-destructive">{t("errorSave")}</p>}` → `{errorKey && <p role="alert" className="text-sm text-destructive">{t("errorSave")}</p>}` (hide никога не сетва billing ключове, безопасно).

В `status === "hidden"` клона (unhide бутонът): `setError(false)` → `setErrorKey(null)`, и замени error render-а с:
```tsx
        {errorKey && (
          <p role="alert" className="text-sm text-destructive">
            {errorKey === "errorSave" ? t("errorSave") : tb(errorKey === "noSubscription" ? "gate.noSubscription" : "gate.limitReached")}
            {errorKey !== "errorSave" && (
              <>
                {" "}
                <Link href="/profil/dostavchik/abonament" className="underline">{tb("gate.cta")}</Link>
              </>
            )}
          </p>
        )}
```

- [ ] **Стъпка 2 (gate):** `pnpm lint && pnpm build` чисти (няма компонентни unit тестове в тази база — виж резолвнато решение #4; поведението се верифицира в Задача 13).

- [ ] **Стъпка 3 (commit):**

```bash
git add src/components/vendor/wizard/step-pregled.tsx src/components/vendor/listing-actions.tsx
git commit -m "feat: publish-gate CTA in step-pregled + listing-actions"
```

---

### Задача 13: E2E walkthrough (M2.1 billing)

**Модел: sonnet** — изпълнява се СЛЕД Задачи 1–12 (целия M2.1 обхват завършен). Модел по образец на `.superpowers/sdd/m1.3-plan-draft/section-e2e.md`.

**Files:**
- Screenshots: `.superpowers/sdd/e2e-shots/m2.1-*.png` — единственият очакван нов артефакт.
- Modify: НИКАКВИ код промени по подразбиране; fix-ове само ако walkthrough-ът намери реален дефект (в рамките на Задачи 1–12 файловете).

**Interfaces:**
- Consumes: `pnpm dev` сървър; dev Neon DB; `createTestSubscription`/`testDb` (`src/test/db-helpers.ts`, Задача 1) за seed на subscription редове (без реални Polar ключове — checkout/webhook пътищата не се тестват end-to-end тук); Playwright MCP browser tools (`mcp__plugin_playwright_playwright__*`).

- [ ] **Стъпка 1 (Гейтове):** `pnpm lint && pnpm test && pnpm build` чисти.

- [ ] **Стъпка 2 (Detached dev server + bounded polling):**

```bash
nohup pnpm dev > /tmp/m2.1-e2e-dev.log 2>&1 &
disown
echo "dev PID: $!"
```

```bash
for i in $(seq 1 20); do
  curl -s -o /dev/null -w "%{http_code}" http://localhost:3000/ | grep -q "200\|307" && break
  sleep 3
done
```

Ако след 20 опита сървърът не отговаря → провери `/tmp/m2.1-e2e-dev.log`, BLOCKED с извадка (не продължавай на сляпо).

**Locale gotcha:** постави `NEXT_LOCALE=bg` cookie (`document.cookie=...` през `browser_evaluate`) ПРЕДИ първата навигация — иначе `Accept-Language` redirect-ва към `/en`.

- [ ] **Стъпка 3 (ЕДНО `ToolSearch` за всички Playwright tools):**

```
ToolSearch(query: "select:mcp__plugin_playwright_playwright__browser_navigate,mcp__plugin_playwright_playwright__browser_snapshot,mcp__plugin_playwright_playwright__browser_click,mcp__plugin_playwright_playwright__browser_type,mcp__plugin_playwright_playwright__browser_fill_form,mcp__plugin_playwright_playwright__browser_take_screenshot,mcp__plugin_playwright_playwright__browser_evaluate,mcp__plugin_playwright_playwright__browser_resize,mcp__plugin_playwright_playwright__browser_tabs,mcp__plugin_playwright_playwright__browser_close")
```

- [ ] **Стъпка 4 (тестов акаунт + billing state seed).**

1. Регистрирай нов vendor акаунт през `/registracia` (UI формата), запомни неговия `user.id`
   (провери в БД: `select id from "user" where email = '<използвания email>'`, или чрез `browser_evaluate`
   четене на session cookie/`authClient.getSession()` в конзолата).
2. Публикувай поне 1 обява през vendor wizard-а (`createDraft` → `submit`) — **ще fail-не с
   `noSubscription` CTA** (виж Стъпка 5, т. 4) при първи опит, ПРЕДИ да сеидваме subscription — това е
   очаквано и е самата проверка на publish gate-а.
3. Seed на billing състояния — временен tsx скрипт (изтрива се след walkthrough-а, НЕ се commit-ва),
   импортиращ съществуващите test хелпъри:

```ts
// /tmp/seed-billing-e2e.ts (temp, НЕ в src/ или scripts/)
import { createTestSubscription, testDb } from "@/test/db-helpers";
import { subscription } from "@/db/schema";
import { eq } from "drizzle-orm";

const userId = process.argv[2];
const plan = process.argv[3] as "standard" | "premium";
const status = process.argv[4] as "active" | "past_due";
const graceUntil = status === "past_due" ? new Date(Date.now() + 3 * 86400_000) : undefined;

await testDb.delete(subscription).where(eq(subscription.userId, userId)); // idempotent re-seed
await createTestSubscription(userId, { plan, status, graceUntil });
console.log("seeded", { userId, plan, status });
```

Изпълнение: `pnpm exec tsx /tmp/seed-billing-e2e.ts <userId> standard active` (пример).

- [ ] **Стъпка 5 (Сценарий).** Всяка точка = screenshot в `.superpowers/sdd/e2e-shots/m2.1-NN-*.png`.

1. Vendor БЕЗ subscription ред → `/profil/dostavchik/abonament` показва `noSubscriptionTitle` +
   `PlanPricingTable` с checkout бутони (без picker, без status card детайли) →
   screenshot `m2.1-01-no-subscription.png`.
2. Publish gate: vendor (без subscription) отваря draft обявата си → `pregled` стъпка → клик "Публикувай"
   → inline banner "Трябва активен абонамент…" + Link "Виж абонамента" → screenshot
   `m2.1-02-publish-gate-no-subscription.png`. Клик на линка → навигира до `/profil/dostavchik/abonament`.
3. Seed `createTestSubscription(userId, {plan:"standard", status:"active"})` → refresh `/profil/dostavchik/abonament`
   → `SubscriptionStatusCard` показва план "Standard" + статус "Активен" + бутон "Управление на плащанията"
   → screenshot `m2.1-03-active-standard.png`.
4. Публикувай обявата (submit) сега минава (1 обява, лимит=1) → success toast → screenshot
   `m2.1-04-publish-succeeds.png`.
5. Опитай да публикуваш ВТОРА обява (стандартен план, лимит=1 общо) → inline banner
   "Достигнат е лимитът на плана ти…" + CTA → screenshot `m2.1-05-limit-reached.png`.
6. Seed `status:"past_due", graceUntil: now+3d` → refresh `/profil/dostavchik/abonament` → past_due warning
   banner с форматирана дата (`Intl.DateTimeFormat` bg locale, `dateStyle:"long"`) → screenshot
   `m2.1-06-past-due-warning.png`.
7. Downgrade picker: seed 2 hidden-system обяви на нов standard vendor (директно през testDb —
   `status:"hidden", hiddenBySystem:true` на 2 предварително публикувани обяви) → refresh abonament страницата
   → `DowngradeListingPicker` се показва автоматично (`plan==="standard" && systemHidden.length>1`) →
   избери 1 radio → "Публикувай избраната" → picker-ът изчезва, избраната обява е published, другата
   остава hidden → screenshot `m2.1-07-downgrade-picker.png`.
8. Restore едно-кликово: premium vendor с 1 hidden-system обява в лимит → клик "Възстанови обявите" →
   обявата се публикува, systemHidden секцията изчезва → screenshot `m2.1-08-restore-listings.png`.
9. Checkout бутон: на `PlanPricingTable`, логнат vendor БЕЗ subscription → бутонът "Абонирай се" е
   рендериран и **enabled** (провери `disabled` атрибут отсъства/false през `browser_snapshot`) — **БЕЗ клик**
   (води извън приложението към Polar hosted checkout) → screenshot `m2.1-09-checkout-button.png`.
10. `dostavchik/layout.tsx` nav: на `/profil/dostavchik/obiavi` и `/profil/dostavchik/abonament` — двата
    таба "Обяви"/"Абонамент" видими, active-state коректен спрямо текущия route (проверка на
    `aria-current="page"` през snapshot) → screenshot `m2.1-10-dostavchik-nav.png`.

- [ ] **Стъпка 6 (Anti-hang правила, спазени по цялото обхождане):**
  - ЕДИН `ToolSearch` за Playwright tools (Стъпка 3).
  - Макс. 2 retry-я на browser call, който не отговаря/timeout-ва; след 2-ри неуспешен опит →
    документирай като defect, продължи (не блокирай целия walkthrough).
  - Polling изчаквания bounded, никога безкраен loop.
  - `browser_close` в края, дори при частичен неуспех.

- [ ] **Стъпка 7 (Cleanup):**

```bash
pkill -f "next dev" || true
rm -f /tmp/seed-billing-e2e.ts
```

Провери port 3000 освободен (`lsof -i :3000` празно). Изтрий тестовите потребители, създадени в
Стъпка 4, през `cleanupTestUser` (малък tsx one-off, същия temp-и-изтрий подход) — dev DB-то е споделено,
не оставяй E2E fixtures.

- [ ] **Стъпка 8 (Поправки):** Всичко счупено по пътя (грешен CTA branch, picker не се показва, restore не
  чисти systemHidden state, nav active-state сгрешен) се поправя в рамките на Задачи 1–12 файловете,
  счупената стъпка се повтаря. Съществени архитектурни проблеми → BLOCKED с описание.

- [ ] **Стъпка 9 (Commit, само ако има fixes):**

```bash
git add -- src/ messages
git commit -m "fix: m2.1 e2e walkthrough findings"
```

- [ ] **Стъпка 10 (Дефект репорт)** — формат по образеца на `.superpowers/sdd/ui-task-9-report.md`:

```markdown
# M2.1 E2E Task 13 — Report

**Env:** Next.js 16.2.10, live Neon dev DB (seeded via createTestSubscription), без реални Polar ключове
(checkout/portal/webhook пътищата НЕ се тестват end-to-end — само рендер/enabled state).
**Result:** PASS / DONE_WITH_CONCERNS / BLOCKED

## Per-стъпка резултати
| # | Стъпка | Резултат | Screenshot |
|---|--------|----------|-----------|
| ... | ... | PASS/FAIL | m2.1-NN-*.png |

## Дефекти
**D1 — [заглавие] (SEVERITY: LOW/MEDIUM/HIGH).**
Repro: ...
Impact: ...

## Не е направено / follow-ups
[...]
```
